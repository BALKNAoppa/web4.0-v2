"use client";

import Image from "next/image";
import Link from "next/link";
import { useEffect, useRef, useState } from "react";
import { usePathname } from "next/navigation";
import {
  ArrowLeft,
  ArrowRight,
  ArrowUpRight,
  Gift,
  Menu,
  MonitorPlay,
  Star,
  Store,
  X,
} from "lucide-react";

import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from "@/components/ui/accordion";
import { SmartLink } from "@/components/layout/smart-link";
import { BrandLogo } from "@/components/layout/brand-logo";
import { LogoHomeLink } from "@/components/layout/logo-home-link";
import {
  menuPromos,
  MENU_PROMOS_HEADING,
  BranchStatusNote,
  LOOKTV_MORPH_TEXTS,
  LanguagePill,
  ProfilePill,
  ThemePill,
  classifierSegments,
  useActiveNavName,
} from "@/components/layout/header-shared";
import {
  appleNavCategories,
  mobileMegaMenus,
  type EcosystemLink,
  type MegaMenu,
} from "@/data/navigation";
import { BRAND } from "@/lib/brand";
import { navType } from "@/lib/nav-type";
import { sectionBg } from "@/lib/section-bg";
import { cn } from "@/lib/utils";
import { SparklesText } from "@/components/ui/sparkles-text";

/**
 * ═══════════════════════════════════════════════════════════════════════
 * MOBILE HEADER — 2 хувилбар
 * ═══════════════════════════════════════════════════════════════════════
 *
 * ⚠️⚠️ ДУГААРЛАЛТ ШИНЭЧЛЭГДСЭН (2026-09-14, захиалагчийн шийдвэр):
 *     хуучин 2 (доод dock)     → ШИНЭ 1
 *     хуучин 3 (burger drawer) → ШИНЭ 2
 *     хуучин 1 (header дээрх ангиллын мөр + таб dropdown) → БҮРМӨСӨН
 *       ХАСАГДСАН. Түүнтэй хамт `BrandTabsHeader`, `BrandTab`, `SectionMenu`,
 *       `SectionRow`, `MenuPromoTeaser` ба `NavigationMenu`-ийн хэрэглээ
 *       бүхэлдээ устсан. Буцааж авах бол git түүхээс (2026-09-14-өөс өмнөх).
 *
 * ⚠️ ТӨСЛИЙН БУСАД ФАЙЛД (`header-shared.tsx`, `navigation.ts`, `nav-type.ts`,
 * `audience-switch.tsx`, `globals.css`) ХУУЧИН дугаараар бичсэн тайлбар
 * үлдсэн — дээрх хөрвүүлэгчээр уншина.
 *
 * ⚠️ DESKTOP нь ХОЁР ХУВИЛБАРТ ЯГ ИЖИЛ (`header.tsx > LogoLeftHeader`).
 * Энэ файл нь ЗӨВХӨН мобайлын давхаргыг л сольдог.
 *
 * ⚠️⚠️ BURGER НЬ ХОЁУЛАА ХУВИЛБАРТ ИЖИЛ (2026-09-14). Хоёулаа
 * `BurgerDrawerHeader`-ийн ХОЁР ДАВХАРГАТ drawer-ыг нээнэ: ангилал → дэд цэс,
 * доод талд нь хэл · профайл · theme. Өмнө нь хувилбар 1 нь баруунаас гарах
 * `Sheet` хэрэглэдэг байсан — тэр код БҮРМӨСӨН устсан.
 * ⇒ ЯЛГАА нь Layer 1-ийн ХЭЛБЭР, Layer 2 ба dock ГУРАВ.
 *
 * ⚠️⚠️ ДУГААР СОЛИГДСОН (2026-09-14 ОРОЙ, захиалагч: "хувилбар 1-ийн хувилбар
 * 2 болгоод хувилбар 2-г хувилбар 1 болго"):
 *     хуучин 1 (хавтгай · Layer 2 · dock) → ШИНЭ 2
 *     хуучин 2 (шилэн капсул)             → ШИНЭ 1
 * Доорх тайлбарууд ШИНЭ дугаараар. Хуучнаар бичсэн түүхэн мөрүүд "(хуучин N)"
 * гэж тэмдэглэгдсэн.
 *
 * ⚠️⚠️ 09-14-НЫ ШИНЭ ЗАГВАРЫН ЗАСВАРУУД ЗӨВХӨН ХУВИЛБАР 2-Т ХАМААРНА
 * (захиалагч: "би зөвхөн [тэр] хувилбар дээр л өөрчлөлт оруул гэсэн юм").
 * Хоёр хувилбарыг ЗЭРЭГЦҮҮЛЖ ҮЗҮҮЛЭХ учраас ХУВИЛБАР 1 нь ЖИШИГ болж
 * хөдлөхгүй байх ёстой.
 *
 * ХУВИЛБАР 2 (хуучин 1) — ХОЁР ДАВХАРГАТ header + доод dock
 *   ┌────────────────────────────────────────────────────┐
 *   │ UNITEL                             ( 👤 )  ( ☰ )   │ L1 — ХАВТГАЙ, шилгүй
 *   ├────────────────────────────────────────────────────┤
 *   │ Багц  Бусад үйлчилгээ  Нэмэлт дата  Гэр инт…  →    │ L2 — хэвтээ гүйнэ
 *   ├────────────────────────────────────────────────────┤
 *   │                     (агуулга)                      │
 *   │  ╭──────────────────────────────────────────────╮  │
 *   │  │  (◉)     ▷      🛍      ☆      ▶            │  │ хөвөгч панел
 *   │  │ Unitel Univision Дэлгүүр Урамшуулал LookTV   │  │ (fixed, 5 таб)
 *   │  ╰──────────────────────────────────────────────╯  │
 *   └────────────────────────────────────────────────────┘
 *   ⚠️ L2 ба dock нь ДАВХАРДАХГҮЙ: dock нь БРЭНД хооронд, L2 нь ТУХАЙН
 *   брэндийн ДОТООД хуудсууд руу (`BrandSubmenuRow`). Дэд цэсгүй ангилал
 *   дээр (Дэлгүүр · Урамшуулал · LookTV) L2 ОГТ рендерлэгдэхгүй.
 *
 * ХУВИЛБАР 1 (хуучин 2) — ХУУЧНААРАА: ШИЛЭН КАПСУЛ, навигаци нь drawer дотор
 *   ┌────────────────────────────────────────────────────┐
 *   │  ╭──────────────────────────────────────────────╮  │
 *   │  │ UNITEL                                ( ☰ )  │  │ `.glass-lens`
 *   │  ╰──────────────────────────────────────────────╯  │ профайлгүй
 *   └────────────────────────────────────────────────────┘
 *   Layer 2-гүй, dock-гүй (`CapsuleRow` + `BurgerDrawerHeader`).
 *
 * ⚠️ ХУУЧИН ХУВИЛБАР 3 (мөн доод tab bar) 2026-08-24-нд нэг удаа хасагдаж
 * байсан. Хувилбар 2-ын доод панел нь түүний кодоос СЭРГЭСЭН (`98f3b43^`) —
 * `data-bottom-tab-bar` зай ба visualViewport-ийн hook. Ялгаа: тэнд 3
 * ангилал + Chat + Профайл байсан; мөн тэр hook нь bar-ыг ГАРААР зөөдөг
 * байсныг 2026-09-08-нд хассан (`useKeyboardOpen` — зөвхөн гар мэдрэх).
 *
 * ТОДОРСОН ангилал — `useActiveNavName` (`header-shared.tsx`): зам таарвал
 * тэр, таарахгүй бол build-ийн домэйн. Desktop-ийн `CategoryNav`, доод bar,
 * burger — ГУРВУУЛАА нэг эх сурвалжаас уншина тул `/devices` рүү орвол бүх
 * давхаргад тодотгол зэрэг зөөгдөнө.
 *
 * Хэлбэр нь БҮХ хувилбарт ИЖИЛ: `text-foreground` + зузаан үсэг + доогуур
 * зураас (light-д хар, dark-д цагаан). Брэнд ногоон (`text-primary`) байхаа
 * больсон — ногоон нь одоо ЗӨВХӨН CTA / promo / Chat-ын дугуйд үлдсэн.
 */

export type MobileVariant = 1 | 2;

/**
 * Капсул доторх ДУГУЙ САААРАЛ burger товч.
 *
 * `size-11` (44px) — WCAG 2.5.8-ын доод хэмжээнээс дээгүүр. Загварт товч
 * үүнээс арай жижиг харагдах ч мобайлд хуруунд таарах нь чухал.
 */
const PILL_ICON_BUTTON =
  "bg-muted hover:bg-muted/70 text-foreground focus-visible:ring-ring inline-flex size-11 shrink-0 items-center justify-center rounded-full transition-colors focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:outline-none";

/**
 * ДООД NAVIGATION-ы табууд — Layer 2-ын БҮХ 5 ангилал.
 *
 * ⚠️ Өмнө нь эдгээр нь ХУУЧИН мобайл хувилбар 1-ийн header дээрх ангиллын
 * мөр байсан. Тэр хувилбар 2026-09-14-нд ХАСАГДСАН тул `TABS` нь одоо
 * ЗӨВХӨН `BottomTabBar`-т хэрэглэгдэнэ.
 *
 * ⚠️ Дараалал `appleNavCategories`-аас өвлөгддөг (доорх `TABS` нь тэрийг
 * шүүдэг), энэ массивын дараалал НӨЛӨӨЛӨХГҮЙ — зөвхөн гишүүнчлэлийг шийднэ.
 */
const TAB_NAMES = ["Unitel", "Univision", "Дэлгүүр", "Урамшуулал", "LookTV"] as const;
type TabName = (typeof TAB_NAMES)[number];

const isTab = (name: string): name is TabName => TAB_NAMES.includes(name as TabName);

const TABS = appleNavCategories.filter((c): c is EcosystemLink & { name: TabName } =>
  isTab(c.name),
);

/**
 * Burger-ийн ШУУД линкүүд — `classifierSegments`-ийн дэд цэсгүй (`brands`-гүй)
 * БҮХ сегмент: Байгууллага → Nexmind, Unitel Group → unitelgroup.mn.
 *
 * ⚠️ Өмнө нь `find((s) => s.id === "business")` гэж ЗӨВХӨН Байгууллага-г
 * нэрээр нь сугалдаг байв. Тиймээс desktop-ийн L1-д шинэ сегмент нэмэхэд
 * (Unitel Group) мобайл burger-т ГАРАХГҮЙ үлдэж, хоёр header чимээгүй зөрж
 * байсан. Шүүлтүүр болгосноор L1-д нэмсэн ЯМАР Ч шууд линк burger-т
 * автоматаар орно — цаашид гараар синк хийх шаардлагагүй. */
const DIRECT_SEGMENTS = classifierSegments.filter((s) => !s.brands?.length);

/**
 * БРЭНДИЙН ТЭМДЭГ — доод navigation-д, `public/`-ийн ЖИНХЭНЭ SVG файлаар.
 *
 * ⚠️⚠️ INLINE `currentColor` ТЭМДГИЙГ СОЛЬСОН (2026-09-14, захиалагч: "dock
 * дээрх Unitel Univision-ы логог `public/Unitel` ба `public/Univision`
 * доторх SVG логоор солиод одоогийн хэмжээг хадгал").
 *
 * ӨМНӨ НЬ: `unitel-icon.svg`-ийн эргэлдэх зурвасыг сугалж, гаднах ногоон
 * squircle-ыг хаяад `currentColor`-оор зурдаг байв. Тэр нь табын төлөвийг
 * ДАГАЖ өнгөө сольдог байсан (идэвхтэй → ногоон · идэвхгүй → бүдэг ·
 * a11y-contrast → шар).
 *
 * ⚠️⚠️ ОДОО ӨНГӨ ТОГТМОЛ. Эдгээр файл нь БҮТЭН апп-дүрс: ногоон squircle +
 * дотор нь тэмдэг. Тиймээс:
 *   · идэвхтэй/идэвхгүй ялгааг ЗӨВХӨН ШОШГО (ногоон ⇄ саарал) заана
 *   · `a11y-high-contrast` (шар) нь эдгээр хоёр дүрсэнд ҮЙЛЧЛЭХГҮЙ —
 *     бусад гурав (lucide) нь хэвээр дагана
 * Энэ нь захиалагчийн шийдвэр; буцаах бол git түүхээс inline хувилбарыг ав.
 *
 * ⚠️ ОПТИК ХЭМЖЭЭ: файлууд нь 382.52×382.52 viewBox-оо БҮТНЭЭР дүүргэсэн
 * squircle тул 24px хайрцагт lucide-ийн зурааснаас ХҮНД харагдана (lucide нь
 * 24-ийн дотор ~20px зурдаг). Захиалагч "одоогийн хэмжээг хадгал" гэсэн тул
 * 24px-ээр ҮЛДЭЭВ — жижигрүүлэх бол `BottomTab`-ийн `size-6`-г эдгээрт л
 * дарж бичнэ, бусад гурвыг хөндөхгүй.
 *
 * `alt=""` — шошго нь дүрсний ШУУД доор бичвэрээр бий (WCAG 1.1.1).
 */
function UnitelMark({ className }: { className?: string }) {
  return <Image src="/Unitel/Unitel100.svg" alt="" width={24} height={24} className={className} />;
}

function UnivisionMark({ className }: { className?: string }) {
  return <Image src="/Univision/UV100.svg" alt="" width={24} height={24} className={className} />;
}

/**
 * ⚠️ LOOKTV-ИЙН ДҮРСНИЙ MORPH ХАСАГДСАН (2026-09-14). Түр зуур `MonitorPlay`
 * ⇄ LookTV-ийн лого (`/Unitel/RGB_Navy_LookTV.png`) уусдаг `LookTvMark`
 * компонент байсныг захиалагч буцаав: "сүүлд хийсэн үйлдлээ буцаа,
 * sparkling-аар үлдээе, логог бас хасъя".
 *
 * ⇒ Dock-ийн LookTV нь ЭНГИЙН `MonitorPlay` глиф + `SparklesText` шошго.
 * `globals.css`-ийн `looktv-mark-a/b` keyframe-үүд ч ХАМТ устсан.
 * Дахин оролдох бол: лого нь wordmark (1500×927, захаараа их хоосон зайтай,
 * navy өнгөтэй) тул 24px хайрцагт уншигдахгүй, dark theme-д ч харагдахгүй —
 * эхлээд КВАДРАТ, монохром хувилбар нь хэрэгтэй.
 */

/**
 * Доод navigation-ы дүрс — ангилал бүрд НЭГ.
 *
 * ⚠️ ХОЁР БРЭНД ӨӨРИЙН ТЭМДГЭЭ ХЭРЭГЛЭНЭ (2026-09-08). Ерөнхий lucide дүрс
 * (`Smartphone`, `Tv`, `CirclePlay`) нь брэндийн табанд ТААРАХГҮЙ — брэндийг
 * хэрэглэгч ТЭМДГЭЭР нь танина, "нэг ямар нэг ухаалаг гар утас"-аар биш.
 * Тиймээс:
 *   Unitel     → `public/Unitel/Unitel100.svg`    (`UnitelMark`)
 *   Univision  → `public/Univision/UV100.svg`     (`UnivisionMark`)
 * ⚠️ Хоёул нь 2026-09-14-нд ЖИНХЭНЭ ФАЙЛ болсон (өмнө нь inline, өнгө
 * дагадаг байв) — дэлгэрэнгүйг `UnitelMark`-ийн тайлбараас үз.
 * Брэндийн БИШ гурав нь lucide хэвээр (загварын дагуу: Дэлгүүр → ДЭЛГҮҮРИЙН
 * ХАЯВЧ `Store`, Урамшуулал → од). LookTV нь `MonitorPlay` — `Tv` чөлөөтэй
 * боловч Univision нь одоо тэмдгээ авсан тул сольж төөрөгдүүлэх шаардлагагүй.
 *
 * ⚠️ `ShoppingBag` → `Store` (2026-09-08). Хоёр шалтгаан: (1) загварын Shop
 * дүрс нь хаявчтай дэлгүүр, цүнх БИШ; (2) цүнхний глиф нь өргөнөөсөө өндөр
 * тул 24×24 хайрцагт 18×20 болж, хажуугийн дөрвөн дүрснээс 10% НАРИЙН
 * харагддаг байв — "дүрсний хэмжээ яг ижил" болох шаардлагатай зөрчилдөж.
 *
 * Төрөл нь `LucideIcon` БИШ — брэндийн тэмдгүүд lucide БИШ, ердөө className
 * авдаг компонент. Ангилал нэмбэл ЭНД БАС нэмнэ (`Record<TabName, …>` тул
 * TypeScript мартуулахгүй).
 */
const TAB_ICONS: Record<TabName, React.ComponentType<{ className?: string }>> = {
  Unitel: UnitelMark,
  Univision: UnivisionMark,
  Дэлгүүр: Store,
  Урамшуулал: Star,
  LookTV: MonitorPlay,
};

/**
 * Доод табын НЭГ хэлбэр — дүрс дээр, шошго доор.
 *
 * Босоо padding нь ЭНД БАЙХГҮЙ — панелийн `py-1.5` нь бүх табд нэг дор зай
 * өгнө. Табын `h-full` нь панелийн өндрийг бүтэн эзэлж, хүрэх талбайг
 * дугуй/шошгоны хэмжээнээс биш ПАНЕЛААС тооцуулна (≈62px > WCAG 2.5.8-ын 44px).
 */
const BOTTOM_TAB =
  "flex h-full w-full flex-col items-center justify-center gap-2 px-0.5 transition-colors";

/**
 * ЗӨВХӨН ЭКРАН ДЭЭРХ ГАР нээгдсэн эсэхийг мэдэрнэ (`data-keyboard`).
 *
 * ⚠️⚠️ 2026-09-08 — ЭНЭ HOOK НЬ BAR-ЫГ БАЙРЛУУЛАХАА БОЛЬСОН.
 *
 * Өмнө нь `useBrowserBottomInset` нэртэй байсан ба `--vv-bottom` хувьсагч
 * бичиж bar-ыг `bottom: var(--vv-bottom)`-ээр ГАРААР дээш зөөдөг байв.
 * Логик нь: "iOS Safari-д `fixed; bottom:0` нь layout viewport-д бэхлэгддэг
 * тул toolbar задрахад bar-ыг дардаг" гэсэн ХУУЧИРСАН таамаг.
 *
 * ОДООГИЙН хөтчүүд (iOS Safari 16+, Android Chrome) `fixed; bottom: 0`-ыг
 * ХАРАГДАХ хэсгийн доод ирмэгт өөрсдөө бэхэлж, address bar хураагдах/задрах
 * үед ХАМТ, ЖИГД гулсуулдаг. Тиймээс JS-ийн нөхөн зөөлт нь хөтчийн ажлын
 * ДЭЭР давхарлаж, bar-ыг хэрэгцээнээс ДЭЭШ өргөж, доор нь ЗАЙ гаргадаг —
 * `transition-[bottom]`-той хамт тэр зай scroll бүрд АНИМАЦТАЙ хөдөлж
 * "bar гарч ирээд буцаж байна" гэсэн шинж үүсгэж байв (захиалагч мэдэгдсэн).
 *
 * Одоо байрлалыг БҮХЭЛД НЬ хөтөч шийднэ (`fixed bottom-0`) — ямар ч
 * төхөөрөмж, ямар ч OS хувилбарт нэг зан төлөв, JS-гүй.
 *
 * ГАР нь ӨӨР асуудал бөгөөс ХЭВЭЭР ҮЛДЭНЭ: гар нээгдэхэд visualViewport
 * ЭРС (250px+) жижигрэх ба bar нь гарын ДЭЭР хөвж, оролтыг халхлах эрсдэлтэй.
 * 150px-ийн хязгаар нь toolbar-ын задралт (40–90px) -аас ДЭЭГҮҮР тул scroll
 * хийхэд хэзээ ч ажиллахгүй — зөвхөн гар нээгдэхэд bar доош гулсаж нуугдана.
 *
 * REACT STATE ХЭРЭГЛЭХГҮЙ — scroll бүрд re-render хийхгүйн тулд `dataset`-д
 * шууд бичнэ (rAF-аар нэгтгэсэн).
 */
function useKeyboardOpen(ref: React.RefObject<HTMLElement | null>) {
  useEffect(() => {
    const vv = window.visualViewport;
    const el = ref.current;
    if (!vv || !el) return;

    let raf = 0;
    const apply = () => {
      const hidden = Math.max(0, Math.round(window.innerHeight - vv.height - vv.offsetTop));
      el.dataset.keyboard = hidden > 150 ? "open" : "closed";
    };
    const update = () => {
      cancelAnimationFrame(raf);
      raf = requestAnimationFrame(apply);
    };

    apply();
    vv.addEventListener("resize", update);
    vv.addEventListener("scroll", update);
    return () => {
      cancelAnimationFrame(raf);
      vv.removeEventListener("resize", update);
      vv.removeEventListener("scroll", update);
    };
  }, [ref]);
}

/**
 * ХӨТЧИЙН ӨӨРИЙН ХЭРЭГСЛИЙН МӨРИЙГ DOCK-ТОЙ НЭГ ӨНГӨ БОЛГОНО (2026-09-11).
 *
 * Захиалагчийн бодит утасны screenshot: dock нь бараан, яг доор нь хөтчийн
 * address bar нь ЦАЙВАР — хоёр зэрэгцсэн зурвас өөр өнгөтэй байснаас dock нь
 * дэлгэцэнд наалдсан биш, "тасарсан" мэт харагдаж байв.
 *
 * ГАНЦ ХӨШҮҮРЭГ нь `<meta name="theme-color">` — хуудас хөтчийн chrome-ын
 * өнгийг СОНГОХ цорын ганц стандарт арга (Chrome/Samsung Internet Android,
 * Safari iOS 15+). CSS-ээс хөтчийн UI-д хүрэх ямар ч зам БАЙХГҮЙ.
 *
 * ⚠️ ЯАГААД `app/layout.tsx`-ийн `viewport.themeColor`-ыг ШУУД СОЛИОГҮЙ ВЭ:
 * тэр нь БҮХ хуудас, БҮХ хувилбарт хүчинтэй статик утга. Dock нь зөвхөн
 * ХУВИЛБАР 2-т, зөвхөн МОБАЙЛД байдаг — хувилбар 1/3 дээр хөтчийн мөр бараан
 * болбол хуудасны цайвар дэвсгэртэй зөрнө. Тиймээс dock ӨӨРӨӨ хариуцна:
 * mount хийгдэхэд солино, unmount-д БУЦААНА.
 *
 * ⚠️ 1024px-ЭЭС ДЭЭШ ХӨНДӨХГҮЙ. Dock нь `lg:hidden` боловч компонент нь
 * desktop дээр ч рендерлэгддэг (зөвхөн CSS-ээр нуугдана). macOS Safari нь
 * theme-color-ыг ТООЛ ДООД мөртөө хэрэглэдэг тул хамгаалалтгүй бол desktop
 * Safari-ийн toolbar шалтгаангүй бараан болно.
 *
 * ⚠️ ӨНГӨ НЬ ОЙРОЛЦООЛОЛ, ЯГ ТААРАХГҮЙ. Dock нь ШИЛ — түүний харагдах өнгө
 * доогуур нь юу гүйж байгаагаас хамаарч ХӨДӨЛНӨ, харин theme-color нь нэг
 * тогтмол утга байх ёстой. Тиймээс хамгийн түгээмэл тохиолдлыг сонгосон:
 * хуудасны ЦЭВЭР дэвсгэр дээрх dock-ийн ДООД ирмэгийн өнгө (хөтчийн мөртэй
 * ЯГ хиллэдэг зурвас). Тооцоо: дэвсгэр → `brightness(1.1)` → `black 72%`
 * тинт → доод `black 26%` градиент.
 *   light  #e2e8ec → #343535
 *   dark   #10131b → #030407
 *
 * ⚠️ `media` нь OS-ийн сонголтыг л уншина — `layout.tsx`-ийн адил хязгаар.
 * Хэрэглэгч `next-themes`-ээр ГАРААР light сонгоод OS нь dark бол хөтчийн
 * мөр бараан хэвээр үлдэнэ. Засах бол theme-г ч хамт сонсох хэрэгтэй.
 */
const DOCK_CHROME_COLOR = { light: "#343535", dark: "#030407" };

function useDockBrowserChrome() {
  useEffect(() => {
    const metas = Array.from(
      document.querySelectorAll<HTMLMetaElement>('meta[name="theme-color"]'),
    );
    if (metas.length === 0) return;

    // Анхны утгуудыг ХАДГАЛНА — unmount-д яг эдгээр рүү буцаана. Hardcode
    // хийвэл `layout.tsx`-ийн өнгө солигдоход энэ файл чимээгүй хоцорно.
    const original = metas.map((m) => m.content);
    const mq = window.matchMedia("(max-width: 1023px)");

    const apply = () => {
      metas.forEach((m, i) => {
        if (!mq.matches) {
          m.content = original[i];
          return;
        }
        m.content = m.media.includes("dark") ? DOCK_CHROME_COLOR.dark : DOCK_CHROME_COLOR.light;
      });
    };

    apply();
    mq.addEventListener("change", apply);
    return () => {
      mq.removeEventListener("change", apply);
      metas.forEach((m, i) => (m.content = original[i]));
    };
  }, []);
}

/**
 * ХУВИЛБАР 2-ЫН ДООД NAVIGATION (dock) — 5 ангилал, БҮТЭН ӨРГӨН, sticky.
 *
 * Хувилбар 1-ийн Layer 1 (ангиллын мөр) нь энд ДООШ зөөгдсөн — захиалагчийн
 * загварын дагуу. Header дээр зөвхөн капсул (лого + burger) үлдэнэ.
 *
 * ⚠️ ХУУЧИН ХУВИЛБАР 3-ААС ЗӨРӨХ нь: тэнд `[Chat]` голдоо, `Профайл` сүүлд
 * байсан бөгөөд ангилал нь ердөө 3 (Unitel · Univision · Дэлгүүр) байв. Одоо
 * БҮХ 5 ангилал — Chat, Профайл, Хайлт ОРООГҮЙ (захиалагчийн шийдвэр):
 * chat нь зүүн ирмэгийн таб, профайл нь burger дотор.
 *
 * ⚠️ `bottom` нь `bottom-0` — ХӨТӨЧ ӨӨРӨӨ шийднэ. Өмнө нь JS-ээр тооцсон
 * `--vv-bottom` байсныг ХАСАВ (дээрх `useKeyboardOpen`-ийн тайлбарыг үз).
 *
 * `data-bottom-tab-bar` — `globals.css` дотор `body:has(…)`-аар хуудасны доод
 * зай гаргахад ашиглагдана (bar нь `fixed` тул footer-ийг дардаг).
 */
function BottomTabBar() {
  const ref = useRef<HTMLElement>(null);
  useKeyboardOpen(ref);
  useDockBrowserChrome();
  const pathname = usePathname() ?? "";

  /**
   * ХАМГИЙН ИХДЭЭ НЭГ таб тодорно. Замтай таарсан таб байвал тэр, эс бөгөөс
   * (жишээ нь нүүр хуудсанд) build-ийн домэйны таб тодорно.
   *
   * `isCurrentPage` (`aria-current`) нь ХАТУУ: нөгөө домэйны таб шинэ tab-аар
   * нээгддэг тул "одоогийн хуудас" болж чадахгүй.
   */
  const isInternal = (t: (typeof TABS)[number]) =>
    !t.owner || t.owner === "self" || t.owner === BRAND;
  const currentName = TABS.find(
    (t) => isInternal(t) && (pathname === t.href || pathname.startsWith(`${t.href}/`)),
  )?.name;
  // Тодотгол нь header-ийн таб, desktop-той НЭГ эх сурвалжаас — гурван
  // давхарга хэзээ ч зөрөхгүй.
  const highlightedName = useActiveNavName(appleNavCategories);

  return (
    /**
     * ⚠️⚠️ ХӨВӨГЧ ПАНЕЛ → БҮТЭН ӨРГӨН DOCK (2026-09-11, захиалагч: "доод nav
     * буюу dock-г дэлгэцэн дээр full width-тэй хий, тэр нь sticky байдлаар
     * байрлана").
     *
     * Өмнө нь `<nav>` нь ЗӨВХӨН ХҮРЭЭ байсан (зайг гаргах хайрцаг,
     * `pointer-events-none`) ба `<ul>` нь Figma-гаас тогтмол 278×73 хар
     * КАПСУЛ болж түүний дотор хөвдөг байв. Одоо `<nav>` ӨӨРӨӨ харагдах
     * панел: ирмэгээс ирмэг хүртэл (`inset-x-0`), доод ирмэгтээ ШАХАЖ
     * суусан. Тиймээс:
     *   `pointer-events-none/auto` ХОС ХАСАГДСАН — nav-д одоо панелийн ГАДНА
     *     "үхсэн" талбай үлдэхгүй тул доорх хуудасны дарагдалтыг залгих
     *     асуудал байхгүй.
     *   `mx-auto w-[278px] max-w-[…]` ХАСАГДСАН — өргөн нь дэлгэцийнх.
     *   `pb-[max(2rem,env(…))]` БҮРЭН ХАСАГДСАН — Figma-ийн 32px зай нь панелийг
     *     ХӨВҮҮЛЭХ зориулалттай байсан бөгөөд full-width dock нь доод ирмэг
     *     дээрээ суух тул утгагүй. Түр зуур `pb-[env(safe-area-inset-bottom)]`
     *     болгож үлдээснийг 2026-09-11-нд ХАСАВ (захиалагч: "доороос нь safe
     *     zone зай авч байна, түүнийг алич") — дэлгэрэнгүйг `globals.css`-ийн
     *     `body:has([data-bottom-tab-bar])` дүрмийн тайлбарт. Товчхондоо:
     *     `viewport-fit=cover` тавиагүй тул тэр `env()` нь ҮРГЭЛЖ 0 байсан,
     *     мөн хөтөч доод хэрэгслийн мөрөө layout viewport-оос аль хэдийн
     *     хасдаг тул давхар тоолол болно.
     *   `rounded-[28px]` → `rounded-t-[28px]` — ЗӨВХӨН ДЭЭД булан. Доод булан
     *     нь дэлгэцийн ирмэгээс гадна үлдэх тул дугуйруулах нь харагдахгүй
     *     атлаа хар дэвсгэрийг зүсэж, доод буланд хуудасны өнгө цухуйна.
     *
     * ⚠️ `fixed` нь `sticky` БИШ гэдгийг САНА. Захиалагчийн "sticky" гэдэг нь
     * "гүйлгэхэд доод ирмэгтээ наалдаж үлдэх" гэсэн зан төлөв — түүнийг
     * `fixed` ЯГ хийж байгаа. Жинхэнэ `position: sticky` нь эцгийнхээ
     * scroll хүрээнд л ажилладаг тул footer хүртэл гүйлгэхэд dock ДЭЭШ
     * гүйж алга болно. ҮҮНИЙГ БҮҮ СОЛИ.
     *
     * ⚠️⚠️ ХӨТЧИЙН ADDRESS BAR-ТАЙ ХЭРХЭН ЗОХИЦОХ ВЭ (2026-09-11-ний асуулт).
     * `fixed bottom-0` нь ЯГ ҮҮНИЙГ хийдэг бөгөөд ЭНЭ НЬ ХАМГИЙН ЗӨВ АРГА:
     * мобайл хөтөч гүйлгэх үед address bar-аа хураахад layout viewport нь
     * ӨӨРӨӨ өндөрсөж, dock түүнтэй хамт доошоо ЖИГД гулсана (хөтчийн өөрийн
     * анимацаар, бидний CSS transition-оос ҮЛ ХАМААРАН). Дэлгэц эргүүлэх,
     * цонх сунгах үед ч ижил.
     *
     * ⚠️ ТИЙМЭЭС JS-ЭЭР БАЙРЛУУЛАХ ГЭЖ БҮҮ ОРОЛД. 2026-09-08 хүртэл
     * `visualViewport`-оос уншсан `--vv-bottom` хувьсагч байсан бөгөөд яг
     * энэ шалтгаанаар ХАСАГДСАН: хөтөч аль хэдийн хийчихсэн зайг JS дахин
     * нэмж, dock хэрэгцээнээс ДЭЭШ өргөгдөж, доор нь хоосон зурвас гарч
     * байв. Түүхийг `useKeyboardOpen`-ийн тайлбарт бүтнээр нь бичсэн.
     *
     * Хөтчийн доод UI-аас ГАНЦ ҮЛ ХАМААРАХ тохиолдол нь ЭКРАН ДЭЭРХ ГАР —
     * түүнийг байрлалаар БИШ, `data-keyboard=open` дээр dock-ийг БҮТНЭЭР
     * нуух замаар шийдсэн (доорх `translate-y-full`).
     */
    <nav
      ref={ref}
      data-bottom-tab-bar
      aria-label="Доод цэс"
      /**
       * ⚠️ БАЙРЛАЛ — `fixed inset-x-0 bottom-0`, JS-ГҮЙ (2026-09-08).
       * Өмнө нь `style={{ bottom: "var(--vv-bottom, 0px)" }}` байв: JS нь
       * хөтчийн доод UI-ийн өндрийг тооцоод bar-ыг тэр хэмжээгээр дээш
       * зөөдөг байсан. Одоогийн хөтчүүд тэрийг ӨӨРСДӨӨ, address bar-тай
       * ЖИГД хамт хийдэг тул JS нь давхарлаж, bar-ыг хэрэгцээнээс дээш
       * өргөж, доор зай гаргаж байв. Дэлгэрэнгүйг `useKeyboardOpen`-д.
       *
       * ⚠️ `transition-[bottom,…]` ч ХАСАГДСАН. `bottom` нь одоо ТОГТМОЛ 0
       * тул хөдлөх зүйл байхгүй; түүнийг сонсох нь зөвхөн хөтчийн жигд
       * гулсалт дээр ӨӨР 300ms анимаци давхарлаж, "bar гарч ирж байна"
       * гэсэн шинж үүсгэж байсан. Одоо зөвхөн ГАРЫН нуулт анимацтай
       * (`translate` + `opacity`) — Tailwind v4-т `translate-y-full` нь
       * `transform` БИШ, `translate` property.
       *
       * ⚠️⚠️ `bg-black shadow-2xl` → `.glass-dock` + `backdrop-blur-2xl`
       * `backdrop-saturate-200 backdrop-brightness-110` (2026-09-11, захиалагч:
       * "хувилбар 2-ын dock буюу bottom nav-г liquid glass effect-тэй болго",
       * дараа нь бодит утасны screenshot-той хамт "хэтэрхий хуучны харагдаж
       * байна, бодит ч юм шиг болгомоор байна").
       *
       * ⚠️ ГУРВУУЛАА ХЭРЭГТЭЙ, blur нь ганцаараа ХАНГАЛТГҮЙ:
       *   `blur-2xl` (40px)      — хэлбэрийг уусгана
       *   `saturate-200`         — доорх өнгийг АМЬД байлгана. Үүнгүйгээр
       *     80% тинт нь бүх өнгийг саарал болгож, "бохир шил" болно.
       *   `brightness-110`       — тинтний дор дарагдсан гэрлийг буцааж авна.
       *     Энэ хоёргүйгээр шил нь зүгээр л БҮДЭГ ХАВТАН шиг харагддаг —
       *     бодит утасны screenshot дээр яг тэр асуудал илэрсэн.
       *
       * Тинт, ирмэгийн тусгал, сүүдэр нь `globals.css > .glass-dock`-д —
       * класс нь шилний ЗУЗААНЫГ, Tailwind нь blur-ыг эзэмшинэ. Тэнд дараах
       * зүйлийг дэлгэрэнгүй бичсэн: яагаад `.glass-capsule`-ыг дахин
       * ашиглаагүй, тинт яагаад 80%, хоёр темийн контрастын хэмжилт, гурван
       * буцалт (backdrop-filter дэмжихгүй · reduce-transparency ·
       * a11y-high-contrast).
       *
       * ⚠️⚠️ ПАНЕЛ ОДОО ТЕМ ДАГАНА (2026-09-14, захиалагч: "dock-г dark
       * болон light theme-тэй болгоод"). ӨМНӨ НЬ ЗОРИУД ХАМААРАХГҮЙ байсан
       * бөгөөд тайлбар нь "бараан суурь нь загварын шийдэл" гэдэг байв —
       * захиалагчийн Figma спек (`#FFFFFF · 80%`, Tokens: Auto/Light) түүнийг
       * хүчингүй болгов. Light = цагаан 80%, dark = хар 80%.
       * ⇒ ТИЙМЭЭС шошгоны өнгө ч токен болсон (`BottomTab`-ийг үз): цагаан
       * hardcode нь цагаан шил дээр алга болно.
       *
       * ⚠️ ХҮРЭЭГҮЙ (2026-09-08, захиалагч: "хар bg-ний border болон өнгө
       * зөрж байна"). `border-t` нэмэхгүй. Шилний ДЭЭД ирмэгийн цайвар тусгал
       * (`.glass-dock`-ийн эхний inset shadow) нь зааг болох ажлыг үүрнэ —
       * тэр нь хүрээ БИШ, гэрлийн тусгал тул өнгө зөрөх асуудал давтагдахгүй.
       * a11y-high-contrast-д харин ШАР дээд зураас `globals.css`-ээс орно
       * (тэнд хуудас ч #000 болдог тул зааг үгүй болно).
       */
      className="glass-dock fixed inset-x-0 bottom-0 z-50 rounded-t-[32px] backdrop-blur-2xl backdrop-brightness-110 backdrop-saturate-200 transition-[opacity,translate] duration-300 ease-out data-[keyboard=open]:translate-y-full data-[keyboard=open]:opacity-0 lg:hidden"
    >
      {/**
       * ӨНДӨР НЬ FIGMA-ААС ТОГТМОЛ 73px — ӨРГӨН нь одоо дэлгэцийнх.
       *
       * ⚠️ `h-[73px]` → `h-[var(--dock-h)]` (2026-09-11). Тоо нь `globals.css`
       * -д НЭГ УДАА бичигдэнэ: хуудасны доод зай (`body:has(…)`) ч мөн тэр
       * хувьсагчаас гардаг тул өндрийг сольвол ХОЁУЛАА дагана. Өмнө нь 73px
       * хоёр файлд ТУСДАА бичигдсэн байв.
       *
       * ⚠️ `<nav>` дээр байсан доод safe-area padding ХАСАГДСАН тул dock-ийн
       * НИЙТ өндөр нь одоо ЯГ энэ утга. Padding-ийг эргүүлж нэмэх бол `<nav>`
       * -д тавина, ЭНД БИШ: `box-sizing: border-box` тул нэг элементэд хоёуланг
       * тавибал padding нь агуулгын зайг ИДНЭ (табууд шахагдана).
       *
       * `items-center` + табын `h-full` — өндөр ТОГТМОЛ тул босоо padding
       * тооцох шаардлагагүй, агуулга өөрөө төвлөрнө.
       *
       * ⚠️ Багана нь ОДОО 53.2px БИШ. 390px дэлгэцэнд (390 − 12) / 5 ≈ 75.6px
       * болж ~42%-иар өргөссөн. Үүнээс хамаарах ХОЁР зүйл бий:
       *   1. `navType.bottomTab` (10px) нь 278px-ийн шахалтаас үүдсэн ТААЗ
       *      байсан — тэр хязгаар ОДОО АРИЛСАН, 11px рүү буцаах боломжтой
       *      (`lib/nav-type.ts`-ийн тайлбарыг үз). Захиалагч хэмжээ солих
       *      тухай хэлээгүй тул 10px-ээр ҮЛДЭЭВ.
       *   2. `BottomTab`-ийн шошго баганаасаа халих тооцоо (6.8px) нь одоо
       *      хамаагүй — "Урамшуулал" 60px нь 75.6px-т чөлөөтэй багтана.
       */}
      {/* ⚠️ PADDING нь FIGMA-ГААС: 12px дээш/доош, 16px хажуу тийш
          (2026-09-14 спек). Өмнө нь `px-1.5` (6px) байсан бөгөөд босоо зай
          нь огт байгаагүй — өндрийг табуудын `h-full` дүүргэдэг байв.

          `box-border` (Tailwind-ийн өгөгдмөл) тул `h-[var(--dock-h)]` = 68px
          нь padding-ыг АГУУЛНА: агуулгад 68 − 24 = 44px үлдэнэ. Дүрс 24 +
          зай 8 + шошго 12 = 44 — ЯГ таарна.

          `justify-between` нь Figma-гийн утга боловч 5 таб бүгд `flex-1`
          тул нөлөөгүй: багана аль хэдийн ТЭНЦҮҮ. Ил бичсэн нь спектэй
          зөрөхгүй байхын тулд. */}
      <ul className="flex h-[var(--dock-h)] w-full items-center justify-between px-4 py-3">
        {TABS.map((tab) => (
          /**
           * ⚠️ `min-w-0` ЗАЙЛШГҮЙ. `flex-1` нь `flex: 1 1 0%` боловч flex
           * item-ийн `min-width` нь ӨГӨГДМӨЛӨӨР `auto` — өөрөөр хэлбэл
           * агуулгаасаа нарийсаж чаддаггүй. Түүнгүйгээр "Урамшуулал" 70px,
           * бусад 48px болж, ДҮРСНҮҮД ЖИГД ЗАЙЛАХГҮЙ (сүүлийн хоёр зай 19%
           * өргөн). `min-w-0` нь 5 баганыг ТЭНЦҮҮ болгоно — dock нь бүтэн өргөн
           * болсноос хойш 390px дэлгэцэнд тус бүр ≈75.6px.
           */
          <li key={tab.name} className="min-w-0 flex-1">
            <BottomTab
              tab={tab}
              highlighted={tab.name === highlightedName}
              isCurrentPage={tab.name === currentName}
            />
          </li>
        ))}
      </ul>
    </nav>
  );
}

/**
 * Доод bar-ын нэг ангилал — дэд цэс НЭЭХГҮЙ, дарвал ШУУД тухайн брэнд рүү
 * шилжинэ. Дэд цэсэнд burger-ээс хүрнэ.
 *
 * `SmartLink` — AI туслахын CTA-тай ЯГ ИЖИЛ логик (`resolveHref`): нөгөө
 * домэйн бол шинэ tab-аар нээгдэнэ. Дотоод/гадаадыг ХАРАГДАЦААР ялгахгүй —
 * нэгдсэн эко-системийн зарчим.
 *
 * ⚠️ ТОДОТГОЛ НЬ ЭНД БРЭНД НОГООН — бусад бүх давхаргаас ЯЛГААТАЙ.
 * Файлын толгойд "ногоон нь зөвхөн CTA/promo/Chat-д" гэсэн дүрэм бий бөгөөд
 * доод bar нь түүний ЗӨВШӨӨРӨГДСӨН ГАНЦ ОНЦГОЙ тохиолдол. Шалтгаан:
 *   1. Захиалагчийн загварт идэвхтэй таб нь ногоон дугуй + ногоон шошготой.
 *   2. Панел нь ТОГТМОЛ БАРААН тул `text-foreground` (light-д хар) тэнд
 *      уншигдахгүй, харин цагаан болговол ИДЭВХГҮЙ табнаас (цагаан/70)
 *      ердөө тунгалагаараа л ялгарна — тэр нь WCAG 1.4.11-д хүрэхгүй.
 *   3. Доогуур зураас (бусад давхаргын тэмдэг) нь шошго нь 11px, дүрсний
 *      доор байрладаг тул бараг үл мэдэгдэнэ.
 * Ногоон нь `--primary` токен тул a11y-high-contrast-д ШАР болж дагана.
 */
function BottomTab({
  tab,
  highlighted,
  isCurrentPage,
}: {
  tab: EcosystemLink & { name: TabName };
  highlighted: boolean;
  isCurrentPage: boolean;
}) {
  const Icon = TAB_ICONS[tab.name];

  return (
    <SmartLink
      href={tab.href}
      owner={tab.owner}
      aria-current={isCurrentPage ? "page" : undefined}
      className={cn(
        BOTTOM_TAB,
        /**
         * ⚠️⚠️ `text-white/65` → ТОКЕН (2026-09-14). Dock нь одоо ТЕМ ДАГАНА
         * (`globals.css > .glass-dock`) тул цагаан hardcode нь light темд
         * цагаан шил дээр АЛГА БОЛНО. `text-muted-foreground` нь Figma-ийн
         * "Text/text-secondary", `text-foreground` нь "Text/text-primary".
         *
         * ⚠️ ИДЭВХТЭЙ нь ГУРВУУЛАА НЭГ ӨНГӨ (дүрс · хүрээ · шошго) —
         * `ring-current` тул хүрээ өнгөө эндээс өвлөнө. Figma-д идэвхтэй
         * ШОШГО нь хар (text-primary), дүрс нь ногоон; захиалагч хүрээг
         * ҮЛДЭЭХ гэж шийдсэн тул гурвыг нэг ногоон болгож үлдээв — эс
         * бөгөөс хар шошго + ногоон хүрээ гэсэн ХОЁР тэмдэг зэрэг харагдана.
         */
        highlighted ? "text-primary" : "text-muted-foreground hover:text-foreground",
      )}
    >
      {/* ⚠️⚠️ ХЭМЖЭЭ FIGMA-ААС (2026-09-14): ДҮРС 24px. Өмнө нь 36px дугуй
          дотор 22px дүрс байв — тэр нь 73px панелийн сул зайг дүүргэх тооцоо
          байсан бөгөөд 68px hug дээр БАГТАХГҮЙ.

          ⚠️⚠️ ДУГУЙ ХҮРЭЭ БҮРМӨСӨН ХАСАГДСАН (2026-09-14, захиалагч: "Unitel-ийн
          лого гадуурх идэвхжсэн мэт хүрээг хас"). Тэр өдөр эхлээд "хүрээг
          үлдээе" гэж шийдэгдээд, бодит рендерийг хараад ХАСАГДСАН — хүрээ нь
          "энэ товч дарагдсан/идэвхжсэн" гэсэн ТӨЛӨВ заадаг мэт уншигдаж
          байв. Figma-ийн загварт ч хүрээ БАЙХГҮЙ.
          ⇒ Идэвхтэйг ОДОО ЗӨВХӨН ӨНГӨ заана (ногоон дүрс + ногоон шошго,
          `SmartLink`-ийн `text-primary`). Дахин нэмэх санаа гарвал энэ
          мөрийг үз — нэг өдөрт хоёр удаа шийдэгдсэн асуудал.

          ⚠️ ТИЙМЭЭС `span` нь layout-ын хувьд ердөө дүрсний хайрцаг (24px).
          `BOTTOM_TAB`-ийн `gap-2` (8px) нь одоо ЦЭВЭР зайн утга:
          24 + 8 + 12 = 44px = агуулгын бүтэн өндөр.

          Таван дүрсний ОПТИК тэнцвэрийг `BRAND_MARK_PAD` (viewBox-ийн 20%
          зай) аль хэдийн хангасан — нэг класс хангалттай. */}
      <span className="flex size-6 shrink-0 items-center justify-center">
        <Icon className="size-6 shrink-0" />
      </span>
      {/* ⚠️ `truncate` ХАСАГДСАН. Хөвөгч капсулын үед багана ЯГ 53.2px байсан
          тул `truncate` бол "Урамшуулал" → "Урамшуула…" болж таслагдах байв;
          оронд нь шошгыг баганаас ХАЛИХЫГ зөвшөөрсөн (10px-д 6.8px халих ба
          хажуугийн нөөцөд багтдаг гэж хэмжсэн).

          Dock бүтэн өргөн болсноор багана ≈75.6px болж, "Урамшуулал" (60px)
          нь ОДОО БҮРЭН БАГТАНА — халилт үгүй. `truncate` буцааж нэмэх ХЭРЭГГҮЙ
          (хийх ажилгүй атлаа ирээдүйд урт шошго чимээгүй таслах эрсдэлтэй).
          `overflow: visible` (default) — `min-w-0` нь зөвхөн ӨРГӨНИЙГ
          хумина, хайчлахгүй. */}
      <span
        className={cn(
          highlighted ? navType.bottomTabActive : navType.bottomTab,
          "leading-none whitespace-nowrap",
        )}
      >
        {/* sr-only тодотгол нь ХАРАГДАХ тодотголтой ижил байх ёстой — эс
            бөгөөс харааны хэрэглэгч нэгийг, screen reader нөгөөг "одоогийн"
            гэж харна. Тиймээс `highlighted`-ыг шууд дамжуулна. */}
        <TabLabel name={tab.name} isDomain={highlighted} />
      </span>
    </SmartLink>
  );
}

// =====================================================================
// ROUTER — хувилбараар салгана
// =====================================================================
export function MobileBrandHeader({ variant }: { variant: MobileVariant }) {
  /**
   * ⚠️⚠️ BURGER НЬ ХОЁУЛАА ХУВИЛБАРТ ИЖИЛ (2026-09-14). Хоёулаа
   * `BurgerDrawerHeader`-ийн ХОЁР ДАВХАРГАТ drawer-ыг дуудна (ангилал → дэд
   * цэс, доод талд нь хэл · профайл · theme). Өмнө нь нэг нь баруунаас гарах
   * `Sheet` (`MenuSheet`) хэрэглэдэг байсан — тэр код БҮРМӨСӨН устсан.
   *
   * ⚠️⚠️ ДУГААР СОЛИГДСОН (2026-09-14 ОРОЙ, захиалагч: "хувилбар 1-ийн
   * хувилбар 2 болгоод хувилбар 2-г хувилбар 1 болго"). Өдрийн туршид
   * "хувилбар 1" гэж яригдсан ШИНЭ ХАВТГАЙ загвар нь ОДОО ХУВИЛБАР 2;
   * хуучин шилэн капсул нь ХУВИЛБАР 1 болов:
   *     хуучин 1 (хавтгай · Layer 2 · dock)  → ШИНЭ 2
   *     хуучин 2 (шилэн капсул, зөвхөн L1)   → ШИНЭ 1
   * ⇒ ЭНЭ ФАЙЛЫН доорх тайлбарууд ШИНЭ дугаараар бичигдсэн. Хуучин
   * дугаараар бичсэн түүхэн тэмдэглэлүүд нь "(хуучин N)" гэж тэмдэглэгдэнэ.
   *
   * ⇒ ЯЛГАА нь `row` · `subRow` · dock ГУРАВ:
   *     Хувилбар 1 — ХУУЧИН шилэн капсул, Layer 2-гүй, dock-гүй
   *     Хувилбар 2 — `row="flat"` (хавтгай + профайл) · Layer 2 · доод dock
   *
   * ⚠️ ХУВИЛБАР 1-ИЙГ БҮҮ ХӨНД (захиалагч: "би зөвхөн [шинэ загварын]
   * хувилбар дээр л өөрчлөлт оруул гэсэн юм"). Хоёрыг зэрэгцүүлж үзүүлэх
   * учраас тэр нь ЖИШИГ — шинэ загварын засварыг ХУВИЛБАР 2-т буулгана.
   */
  if (variant === 1) return <BurgerDrawerHeader />;

  /**
   * ХУВИЛБАР 2 — ХОЁР ДАВХАРГАТ header + доод dock (захиалагчийн 09-14-ны
   * screenshot):
   *   Layer 1 — лого · профайл · burger  (`HeaderRow`)
   *   Layer 2 — ОДООГИЙН брэндийн дэд цэс, хэвтээ гүйдэг (`BrandSubmenuRow`)
   *   доод   — брэнд СОЛИХ dock (`BottomTabBar`)
   *
   * ⚠️ ХОЁР ДАВХАРГА ДАВХАРДАХГҮЙ: dock нь БРЭНД хооронд (Unitel ⇄ Univision
   * ⇄ Дэлгүүр …), Layer 2 нь ТУХАЙН брэндийн ДОТООД хуудсууд руу.
   */
  return (
    <>
      <BurgerDrawerHeader row="flat" subRow={<BrandSubmenuRow />} />
      <BottomTabBar />
    </>
  );
}

/**
 * БӨӨРӨНХИЙ ШИЛЭН КАПСУЛ — лого + burger. ЗӨВХӨН ХУВИЛБАР 1.
 *
 * ⚠️⚠️ ШИНЭ ЗАГВАРААС САЛСАН (2026-09-14, захиалагч: "хувилбар [1]-ийн
 * header-ийн хуучин bg-г гарга, би зөвхөн [нөгөө] хувилбар дээр л өөрчлөлт
 * оруул гэсэн юм"). Тэр өдөр эхлээд ХОЁУЛАНГААС нь шил хассан байсныг буцаав
 * — хоёр хувилбарыг зэрэгцүүлж ҮЗҮҮЛЭХ учраас ХУВИЛБАР 1 нь ХӨНДӨГДӨӨГҮЙ
 * байх ёстой. ⇒ ЭНЭ КОМПОНЕНТЫГ БҮҮ "ЦЭВЭРЛЭ": хоёр мөр төстэй харагдах ч
 * тэдгээр нь ХОЁР ӨӨР хувилбарын загвар.
 *
 * ⚠️ ПРОФАЙЛ ТОВЧГҮЙ (2026-09-07, захиалагч: "профайл нь burger дотор
 * орно"). Хувилбар 2-ын `HeaderRow` дээр профайл БИЙ — тэр нь шинэ
 * загварынх; энд нэмбэл хувилбар 1 ч өөрчлөгдөнө.
 *
 * ⚠️ ШИЛНИЙ ХАРАГДАЦ: `bg-card`+`shadow-sm` (09-07) → `.glass-capsule`
 * (09-10) → **`.glass-lens`** (2026-09-15, захиалагч: "mobile хувилбар 1
 * дээр header container дээр энэ style-г ашигла"). Desktop-ийн капсултай
 * ЯГ НЭГ эх сурвалж (globals.css) — Figma-гийн "Glass" пресет. Хэлбэрийг
 * (өндөр, радиус, padding) нь энд, ХАРАГДАЦЫГ нь тэнд гэж хуваасан.
 * ⇒ `.glass-capsule` нь ОДОО ХААНА Ч ХЭРЭГЛЭГДЭХГҮЙ болж устсан.
 *
 * ⚠️ Логонь ҮГЭН лого (`BrandLogo`), эко тэмдэг (`BrandLogoLink`) БИШ.
 * Footer-ийнхтэй ижил хослол тул nested anchor үүсэхгүй.
 */
function CapsuleRow({ burger }: { burger: React.ReactNode }) {
  return (
    <div className="px-4 py-3">
      <div className="glass-lens flex h-16 items-center rounded-full px-5">
        <LogoHomeLink className="inline-flex items-center" aria-label="Нүүр">
          <BrandLogo height={24} preload />
        </LogoHomeLink>

        {/* BURGER — ангиллын дэд цэс, Байгууллага, тохиргоо. Профайл нь
            drawer доторх мөр болж орсон — капсул дээр тусдаа товч БАЙХГҮЙ. */}
        <div className="ml-auto flex items-center">{burger}</div>
      </div>
    </div>
  );
}

/**
 * LAYER 1 — ЛОГО (зүүн) · ПРОФАЙЛ + BURGER (баруун). ЗӨВХӨН ХУВИЛБАР 2.
 *
 * ⚠️⚠️ ХАВТГАЙ, ШИЛГҮЙ (2026-09-14, захиалагчийн screenshot). Капсулын
 * дугуйрал, тусгал, сүүдэр гурвуулаа алга — header нь хуудасны дэвсгэр
 * дээр ШУУД суудаг. Тиймээс:
 *   · гадна `px-4 py-3` бүрхүүл ХЭРЭГГҮЙ (капсулыг хөвүүлэх зориулалттай байв)
 *   · `rounded-full` · `.glass-capsule` БАЙХГҮЙ
 *   · өндөр нь 64px хэвээр — лого 24px + хүрэх талбай
 * ⚠️ ХУВИЛБАР 1 нь ХУУЧИН ШИЛЭН КАПСУЛТАЙГААРАА ҮЛДСЭН (`CapsuleRow`) —
 * хоёрыг нэгтгэх гэж БҮҮ оролд.
 *
 * ⚠️ ПРОФАЙЛ ТОВЧ ЭНД БИЙ. 2026-09-07-нд "профайл нь burger дотор орно" гэж
 * хассан байсныг захиалагчийн 09-14-ны screenshot энэ хувилбарт буцаав
 * (лого · 👤 · ☰). Drawer доторх `ProfilePill` нь ХЭВЭЭР — давхардал БИШ:
 * энэ нь хурдан хандалт, тэр нь цэсний мөр.
 */
function HeaderRow({ burger }: { burger: React.ReactNode }) {
  return (
    /**
     * ⚠️ ДЭВСГЭР ЭНД ОГТ ӨГӨГДӨХГҮЙ — `<header>`-ийнхөө өнгийг өвлөнө
     * (2026-09-14: нэг удаа `sectionBg.band` тавьсныг захиалагч БУЦААВ —
     * "би sub menu гэж байхад үндсэн header-ийг өөрчлөөд байна шүү дээ").
     * ⇒ Layer 1-ийн ХАРАГДАЦАД БҮҮ ХҮР. Дэд цэсийг ялгаруулах хэрэгтэй бол
     * ЗӨВХӨН `BrandSubmenuRow`-ийн дэвсгэрийг өөрчил.
     *
     * ⚠️⚠️ PADDING FIGMA-ГААС (2026-09-14): зүүн/баруун 24 · дээш 24 · доош 12.
     *   `px-6 pt-6 pb-3`
     *
     * ⚠️ `h-16` ХАСАГДСАН — өндөр нь одоо АГУУЛГААРАА (hug). 64px тогтмол
     * өндөр нь 24+12 padding-ийг ИДЭЖ, агуулгад 28px л үлдээнэ (44px товч
     * багтахгүй). Одоо 24 + 44 + 12 = 80px.
     *
     * ⚠️ ДООШ 12 нь ДЭЭШ 24-ийн ХАГАС — САНААТАЙ. Доор нь Layer 2 (дэд цэс)
     * шууд залгаж суудаг тул тэнцүү зай өгвөл хоёр мөр хоорондоо хэт
     * холдож, нэг блок мэт уншигдахаа болино.
     */
    <div className="flex items-center px-6 pt-6 pb-3">
      <LogoHomeLink className="inline-flex items-center" aria-label="Нүүр">
        <BrandLogo height={24} preload />
      </LogoHomeLink>

      {/* ⚠️ `gap-1` — хоёр товч хоёулаа 44px хүрэх талбайтай тул тэдний
          хооронд ТОМ зай хэрэггүй; загварт ч наалдуу. */}
      <div className="ml-auto flex items-center gap-1">
        {/* ХЭЛБЭР нь ЭНДЭЭС — `ProfilePill` нь drawer-ийн доод мөрөнд ч
            хэрэглэгддэг (`DRAWER_TOOL_PILL_ACTIVE`) бөгөөд тэнд өргөн pill,
            энд 44px дугуй. Компонент НЭГ, хэлбэр нь дуудагчийнх. */}
        <ProfilePill className={PILL_ICON_BUTTON} />
        {burger}
      </div>
    </div>
  );
}

/**
 * LAYER 2 — ОДООГИЙН БРЭНДИЙН ДЭД ЦЭС, ХЭВТЭЭ ГҮЙДЭГ (зөвхөн ХУВИЛБАР 2).
 *
 * (2026-09-14, захиалагч: "2дахь layer-т хэрвээ Unitel-д байгаа бол
 * Unitel-ийн sub menu-г оруулъя".)
 *
 * ⚠️ АЛЬ БРЭНД ГЭДГИЙГ `useActiveNavName` шийднэ — desktop-ийн `CategoryNav`,
 * доод dock, burger гурвуулаа ИЖИЛ эх сурвалж. Замаар таарсан нь байвал тэр,
 * эс бөгөөс build-ийн домэйн (`BRAND`).
 *
 * ⚠️ ДЭД ЦЭСГҮЙ АНГИЛАЛД ОГТ РЕНДЕРЛЭГДЭХГҮЙ (`Дэлгүүр`, `Урамшуулал`,
 * `LookTV` — `mobileMegaMenus`-д бичлэггүй). Хоосон 44px зурвас үлдээвэл
 * header тэр хуудсууд дээр шалтгаангүй өндөр болно.
 *
 * ⚠️ МӨРНИЙ АГУУЛГА нь САЛААНЫ ГАРЧИГ БИШ, НАВЧ мөрүүд. Unitel-ийн дэд цэс
 * нь ердөө ХОЁР салаатай ("Үндсэн багцууд" · "Бусад үйлчилгээ") тул
 * гарчгуудыг тавибал хоёрхон үг гарч, гүйх мөр болохгүй. Навчийг (Premium
 * багц · Priority · Plus · Smart Data … Гэр интернет · Нэмэлт дата багц …)
 * дэлгэснээр захиалагчийн загвар дээрх шиг хажуу тийш гүйдэг жинхэнэ дэд цэс
 * болно. Навч бүр ӨӨРИЙН `href`-тэй тул мөр нь шууд хүрэх цэг.
 *
 * ⚠️⚠️ НАВЧГҮЙ САЛАА нь ӨӨРӨӨ мөрөнд орно (2026-09-14, Univision дээр
 * илэрсэн алдаа). Univision-ы дэд цэсний ДӨРВӨН салааны хоёр нь ЗОРИУД
 * навчгүй: "Үндсэн бүтээгдэхүүн" (`status: "link-only"` — цэвэр линк) ба
 * "Life-style" (`coming-soon`). Зөвхөн навч цуглуулбал тэр хоёр нь ЧИМЭЭГҮЙ
 * УНАЖ, Univision-ы мөрөнд гол хоёр орц нь ГАРАХГҮЙ байв. Тиймээс дүрэм:
 *     навчтай салаа → навчууд нь · навчгүй салаа → САЛАА ӨӨРӨӨ
 * Ингэснээр Unitel-ийн бүтэц ч хөндөгдөхгүй (хоёр салаа нь хоёулаа навчтай).
 */
function BrandSubmenuRow() {
  const activeName = useActiveNavName(appleNavCategories);
  const menu = mobileMegaMenus[activeName];
  if (!menu) return null;

  const items = menu.sections.flatMap((branch) => {
    const leaves = (branch.groups ?? []).flatMap((group) => group.items);
    return leaves.length > 0 ? leaves : [{ id: branch.id, title: branch.title, href: branch.href }];
  });
  if (items.length === 0) return null;

  return (
    /**
     * ⚠️ ДЭВСГЭР нь `band` = `bg-card` (#f3f5f7) — header-ийн `bg-background`
     * (#e2e8ec, САААРАЛ) -аас НЭГ ШАТ ЦАЙВАР (2026-09-14, захиалагч: "sub
     * menu-ийн bg-г design дээрх шиг өнгөтэй болго, одоо байгаа нь
     * ялгарахгүй байна").
     *
     * ⚠️ ЯАГААД ЦАЙВАР, харин загвар дээрх шиг БАРААН БИШ ВЭ: загварт Layer 1
     * нь ЦАГААН, дэд цэс нь түүнээс бараан саарал. Гэвч Layer 1-ийг цагаан
     * болгох нь ҮНДСЭН HEADER-ийг өөрчилнө — захиалагч түүнийг ЗОРИУД
     * хориглосон. Header ба доорх агуулга хоёулаа #e2e8ec тул палитрын дотор
     * ялгарах ГАНЦ зам нь ЦАЙВРУУ явах. Layer 1-ийг цагаан болгохыг
     * зөвшөөрвөл энэ мөрийг `sectionBg.page` болгож загвартай яг таарна.
     *
     * ⚠️ ХҮРЭЭ ХАСАГДСАН. Өмнө нь `border-t` нь хоёр мөрийг заагладаг байсан
     * бөгөөд хоёулаа ижил өнгөтэй үед ТЭР Л ганц зааг байв. Одоо өнгөний
     * шат өөрөө зааглах тул зураас нэмбэл давхар зааг болно (09-08-ны
     * "хар bg-ний border болон өнгө зөрж байна" гэсэн зарчимтай нэг).
     *
     * ⇒ ДООШОО ҮРГЭЛЖИЛНЭ: хуудасны биет (PromoHero → ChatHero) ч мөн
     * `page` тул дэд цэсний зурвас нь агуулгатайгаа ТАСАЛДАЛГҮЙ нийлнэ —
     * загварт ч тэгж харагдана.
     */
    <nav aria-label={`${activeName} дэд цэс`} className={sectionBg.band}>
      {/* MASK нь ГҮЙЛГЭХ БОЛОМЖИЙН ТЭМДЭГ: `no-scrollbar` нь scrollbar-ыг
          нуудаг тул нарийн дэлгэцэнд "гүйлгэж болно" гэдэг нь харагдахгүй
          байв. Ирмэг дээр 12px-д бүдгэрнэ — захиалагчийн загварт ч баруун
          тал нь ЗОРИУД тасарч харагддаг. */}
      <div className="no-scrollbar overflow-x-auto [mask-image:linear-gradient(to_right,transparent,black_12px,black_calc(100%-12px),transparent)]">
        {/* ⚠️ `px-6` = Layer 1-ийн гутертэй ЯГ ИЖИЛ 24px (2026-09-14). Эхний
            зүйл ("Багц") нь ЛОГОНЫ зүүн ирмэгтэй босоо шугамд эгнэх ёстой —
            16px үлдээвэл дэд цэс нь 8px зүүн тийш халиад "хазайсан" мэт
            харагдана. Layer 1-ийн padding сольвол ЭНЭ Ч ХАМТ солигдоно. */}
        <ul className="flex w-max items-center gap-5 px-6 py-3">
          {items.map((item) => (
            <li key={item.id}>
              <SmartLink
                href={item.href}
                className={cn(
                  navType.mobileLink,
                  "text-foreground hover:text-primary whitespace-nowrap transition-colors",
                )}
              >
                {item.title}
              </SmartLink>
            </li>
          ))}
        </ul>
      </div>
    </nav>
  );
}

/**
 * КАПСУЛЫН ДУГУЙ BURGER ТОВЧ — гурван хувилбарт ИЖИЛ.
 *
 * `open` нь ЗӨВХӨН хувилбар 3-д хэрэглэгдэнэ (drawer-ийн төлөвийг харуулж
 * `Menu` ⇄ `X` солино). Хувилбар 1/2-ын `Sheet` нь дарагдмагц бүтэн дэлгэцээр
 * нээгддэг тул trigger нь харагдахаа болино — тэнд X хэрэггүй.
 *
 * `...rest` нь ХАМГИЙН СҮҮЛД: `SheetTrigger asChild` нь `onClick`,
 * `aria-expanded`, `data-state` зэргийг ЭНД дамжуулдаг тул тэднийг дарж
 * бичихгүй байх ёстой.
 */
function BurgerButton({
  open = false,
  ...rest
}: { open?: boolean } & React.ComponentProps<"button">) {
  return (
    <button
      type="button"
      aria-label={open ? "Цэс хаах" : "Цэс нээх"}
      className={PILL_ICON_BUTTON}
      {...rest}
    >
      {open ? <X className="size-5" /> : <Menu className="size-5" />}
    </button>
  );
}

// =====================================================================
// BURGER DRAWER — ХОЁУЛАА ХУВИЛБАРЫН цэс (хоёр давхаргат, хажуу тийш гулсдаг)
// =====================================================================

/**
 * Drawer-ийн панелийн id — burger-ийн `aria-controls` түүн рүү заана.
 * Хувилбар 3 нь mobile header-т ЗӨВХӨН НЭГ л удаа рендерлэгддэг тул тогтмол
 * id зөрчил үүсгэхгүй (`useId` шаардлагагүй).
 */
const DRAWER_ID = "mobile-burger-drawer";

/**
 * Дэд цэстэй ангилал эсэхийг `mobileMegaMenus`-д бичлэгтэй эсэхээр шийднэ —
 * `header.tsx > CategoryNav`, `BrandTab` -тай ЯГ ИЖИЛ логик.
 *
 * Одоогийн байдлаар: Unitel · Univision → бичлэгтэй ⇒ дэд цэс задарна.
 * Дэлгүүр · Урамшуулал · LookTV → бичлэггүй ⇒ ЭНГИЙН ЛИНК.
 * Тиймээс шинэ ангилалд дэд цэс нэмэхэд ЭНД юу ч засах шаардлагагүй — data
 * дээр `mobileMegaMenus` бичлэг нэмэхэд гурван давхарга зэрэг дагана.
 */
const hasSubmenu = (name: string) => Boolean(mobileMegaMenus[name]);

/** Drawer-ийн 1-р давхаргын ҮНДСЭН мөр (ангилал) — 15px · 600, 44px өндөр */
const DRAWER_PRIMARY_ROW = cn(
  navType.drawerPrimary,
  "hover:bg-muted flex min-h-11 w-full items-center gap-3 rounded-lg px-2 text-left transition-colors",
);

/** Дэд цэсний навчин линк — үндсэн мөртэй ижил зэрэглэл, сумгүй */
const DRAWER_LEAF_ROW = cn(
  navType.drawerPrimary,
  "hover:bg-muted flex min-h-11 w-full items-center gap-3 rounded-lg px-2 text-left no-underline transition-colors",
);

/**
 * ХУВИЛБАР 3 — header дээр ЗӨВХӨН капсул, БҮХ цэс burger-ийн ард.
 *
 * ┌────────────────────────────────────────────┐
 * │  ╭──────────────────────────────────────╮  │
 * │  │ UNITEL                        ( ☰ )  │  │ капсул
 * │  ╰──────────────────────────────────────╯  │
 * │  ╭──────────────────────────────────────╮  │
 * │  │ Unitel                            →  │  │ ← 1-р давхарга
 * │  │ Univision                         →  │  │
 * │  │ Дэлгүүр                              │  │   (дэд цэсгүй ⇒ шууд линк)
 * │  │ Урамшуулал                           │  │
 * │  │ LookTV                               │  │
 * │  │ ──────────────────────────────────── │  │
 * │  │ Theme · Хэл солих · Нэвтрэх          │  │
 * │  │ ──────────────────────────────────── │  │
 * │  │ Байгууллага ↗ · Unitel Group ↗       │  │
 * │  ╰──────────────────────────────────────╯  │
 * └────────────────────────────────────────────┘
 *
 * `Sheet` (radix Dialog) ХЭРЭГЛЭЭГҮЙ ГУРВАН ШАЛТГААН:
 *   1. Загварт панел нь дэлгэцийн хажуугаас БИШ, header-ийн ДООРООС задардаг
 *      бөгөөд өндөр нь АГУУЛГААР тодорхойлогддог. Sheet нь ТОГТМОЛ өндөртэй.
 *   2. Sheet бол modal — фокусыг цоожилж, хуудсыг `aria-hidden` болгодог.
 *      Энэ панел нь цэс (disclosure) бөгөөс хувилбар 1-ийн `NavigationMenu`
 *      -тай ИЖИЛ зан төлөвтэй байх ёстой: Escape/гадна дарж хаагдана, гэхдээ
 *      фокус цоожлохгүй.
 *   3. `ui/sheet.tsx` нь `data-open:`/`data-closed:` гэсэн БАЙХГҮЙ Tailwind
 *      variant хэрэглэдэг тул анимаци нь ОДООГООР ажилладаггүй (тусад нь
 *      зассан ч энэ панелийн хоёр давхаргын гулсалтыг Sheet хийж чадахгүй).
 */
function BurgerDrawerHeader({
  row = "capsule",
  subRow,
}: {
  /**
   * LAYER 1-ИЙН ХЭЛБЭР — ХУВИЛБАР ХООРОНД ЯЛГААТАЙ (2026-09-14):
   *   "capsule" → ХУВИЛБАР 1, хуучин шилэн капсул (ХӨНДӨГДӨӨГҮЙ)
   *   "flat"    → ХУВИЛБАР 2, шинэ хавтгай мөр + профайл товч
   * Хоёулаа `{ burger }` авдаг тул доор компонентыг сонгоод л дуудна.
   */
  row?: "capsule" | "flat";
  /** ХУВИЛБАР 2-ын Layer 2 (брэндийн дэд цэс). Хувилбар 1-д өгөгдөхгүй. */
  subRow?: React.ReactNode;
}) {
  const Layer1 = row === "flat" ? HeaderRow : CapsuleRow;
  const [open, setOpen] = useState(false);
  /** null = 1-р давхарга (үндсэн жагсаалт) · нэр = тэр ангиллын дэд цэс */
  const [subName, setSubName] = useState<string | null>(null);
  /**
   * ⚠️ ХАРАГДАЖ БАЙГАА дэд цэсний АГУУЛГА нь `subName`-ЭЭС ТУСДАА state.
   * `subName` нь БАЙРЛАЛЫГ (аль давхарга) хариуцна, энэ нь АГУУЛГЫГ. Хоёрыг
   * нэгтгэвэл "Буцах" дарахад агуулга ТЭР ДОРОО unmount болж, баруун тийш
   * гулсах анимаци хоосон панелаар гүйнэ. Тиймээс буцсаны дараа ч сүүлд
   * үзсэн цэс нь дэлгэцийн ГАДНА, `inert`-ээр үлдэнэ.
   */
  const [subMenu, setSubMenu] = useState<MegaMenu | null>(null);

  const burgerRef = useRef<HTMLButtonElement>(null);
  const panelRef = useRef<HTMLDivElement>(null);
  const rootPaneRef = useRef<HTMLDivElement>(null);
  const subPaneRef = useRef<HTMLDivElement>(null);
  /** Буцахад фокус аль ангиллын мөрөнд эргэж орох вэ */
  const returnTo = useRef<string | null>(null);

  const [paneH, setPaneH] = useState<number>();
  const pathname = usePathname();
  const activeName = useActiveNavName(appleNavCategories);

  /** Хаах + фокусыг burger руу БУЦААХ (Escape, scrim, товч дарсан үед) */
  const close = () => {
    setOpen(false);
    burgerRef.current?.focus({ preventScroll: true });
  };

  /**
   * Линк дарсан үед — фокус БУЦААХГҮЙ. Шилжилт болж байгаа тул фокусыг
   * burger руу зөөвөл шинэ хуудас "цэс нээх товч" дээр фокустай нээгдэнэ.
   */
  const dismiss = () => setOpen(false);

  /**
   * ЗАМ СОЛИГДВОЛ ХААНА — жишээ нь хөтчийн "Back" товч, эсвэл цэсний гадна
   * ажилласан шилжилт. Панел доторх линкүүд `dismiss`-ээр өөрсдөө хаадаг тул
   * энэ нь ХАМГААЛАЛТ; мөн `href="#"` линкүүд зам СОЛЬДОГГҮЙ тул тэднийг
   * зөвхөн `dismiss` хаана.
   *
   * ⚠️ `useEffect`-ЭЭР БИШ, RENDER-ИЙН ҮЕД тааруулав. `useEffect(() =>
   * setOpen(false), [pathname])` нь эффектийн дотор setState дуудаж, дараалсан
   * (cascading) render үүсгэдэг — `react-hooks/set-state-in-effect` түүнийг
   * зогсоов. Доорх нь React-ийн бичиг баримтад заасан "prop солигдоход
   * state-ыг тааруулах" загвар: React зурахаасаа ӨМНӨ дахин render хийнэ.
   */
  const [seenPath, setSeenPath] = useState(pathname);
  if (pathname !== seenPath) {
    setSeenPath(pathname);
    if (open) setOpen(false);
  }

  // ESCAPE
  useEffect(() => {
    if (!open) return;
    const onKey = (e: KeyboardEvent) => {
      if (e.key !== "Escape") return;
      setOpen(false);
      burgerRef.current?.focus({ preventScroll: true });
    };
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, [open]);

  /**
   * ФОКУСЫН ЗӨӨЛТ — хоёр давхаргат цэсний ГОЛ UX.
   *   нээхэд        → 1-р мөр (Unitel)
   *   дэд цэс нээхэд → "Буцах" товч (шинэ давхаргын эхлэл)
   *   буцахад       → ГАРСАН ангиллынхаа мөр (хаанаас гарснаа "мартахгүй")
   *
   * `preventScroll: true` — гулсах анимацийн дундуур фокуслахад хөтөч
   * элементийг харагдуулах гэж scroller-ыг үсэргэдэг. Панел өөрөө
   * `overflow-y-auto` тул тэр нь агуулгыг дээш "цүүрүүлнэ".
   */
  useEffect(() => {
    if (!open) return;
    const panel = panelRef.current;
    if (!panel) return;

    if (subName) {
      panel.querySelector<HTMLElement>("[data-drawer-back]")?.focus({ preventScroll: true });
      return;
    }
    // Буцах — аль ангиллаас гарсныг `dataset`-аар олно (attribute selector-т
    // нэрийг шигтгэвэл кирилл/тусгай тэмдэгт escape хийх шаардлага гарна).
    const want = returnTo.current;
    const rows = [...panel.querySelectorAll<HTMLElement>("[data-drawer-cat]")];
    const target = want ? rows.find((r) => r.dataset.drawerCat === want) : rows[0];
    (target ?? rows[0])?.focus({ preventScroll: true });
    returnTo.current = null;
  }, [open, subName]);

  /**
   * ӨНДРИЙГ АГУУЛГААР ХӨӨНӨ — "content-ийн хэмжээгээр доошоо extend".
   *
   * Хоёр давхарга нь НЭГ мөрөнд зэрэгцэн сууна (`w-[200%]`). Тиймээс мөрийн
   * өндөр нь ХОЁУЛАНГИЙН ИХ нь болно — гаднах хайрцагт ИДЭВХТЭЙ давхаргын
   * өндрийг ГАРААР онооно.
   *
   * ⚠️ Мөрөнд `items-start` ЗАЙЛШГҮЙ: flex-ийн `align-items` нь өгөгдмөлөөр
   * `stretch` тул хоёр давхарга ХОЁУЛАА мөрийн (=илүү өндөрийн) өндөртэй
   * болж, `offsetHeight` нь агуулгынхаа өндрийг БУЦААХГҮЙ.
   *
   * `ResizeObserver` — агуулга нь дотроо ч өөрчлөгдөж болно (нэвтэрсэн үед
   * профайлын мөр урт болно, шрифт ачаалагдахад мөр өндөрсөнө).
   */
  useEffect(() => {
    if (!open) return;
    const el = subName ? subPaneRef.current : rootPaneRef.current;
    if (!el) return;
    const apply = () => setPaneH(el.offsetHeight);
    apply();
    const ro = new ResizeObserver(apply);
    ro.observe(el);
    return () => ro.disconnect();
  }, [open, subName]);

  const openSub = (name: string) => {
    const menu = mobileMegaMenus[name];
    if (!menu) return;
    returnTo.current = name;
    setSubMenu(menu);
    setSubName(name);
  };

  return (
    <>
      {/* PAGE BLUR — хувилбар 1-ийн scrim-тэй ЯГ ИЖИЛ. `absolute top-full`
          нь `<header>`-ийн ДООД ирмэгээс эхэлнэ: header өөрөө хэзээ ч
          бүдгэрэхгүй, зөвхөн доорх агуулга бүдгэрнэ. `h-lvh` — URL bar
          хураагдсан үед доод тал хоосон үлдэхгүй. */}
      <div
        aria-hidden
        onClick={close}
        className={cn(
          "bg-foreground/10 absolute inset-x-0 top-full z-40 h-lvh backdrop-blur-sm transition-opacity duration-300 ease-out lg:hidden",
          open ? "opacity-100" : "pointer-events-none opacity-0",
        )}
      />

      <div className="lg:hidden">
        <Layer1
          burger={
            <BurgerButton
              ref={burgerRef}
              open={open}
              aria-expanded={open}
              aria-controls={DRAWER_ID}
              onClick={() => {
                if (open) {
                  close();
                  return;
                }
                // Дахин нээхэд ҮРГЭЛЖ 1-р давхаргаас — хэрэглэгч сүүлд
                // хаана байснаа санахгүй, "цэс нээх" нь цэсний ЭХЛЭЛ.
                returnTo.current = null;
                setSubName(null);
                setOpen(true);
              }}
            />
          }
        />

        {/* LAYER 2 — ХУВИЛБАР 1-д л дамжуулагдана (`MobileBrandHeader`).
            ⚠️ Drawer-ийн ДЭЭР, `<header>`-ийн ДОТОР байрлана. Drawer нь
            `absolute top-full` тул энэ мөр орсноор панел ӨӨРӨӨ доошоо
            зөөгдөж, Layer 2-ыг дарахгүй — өндрийг px-ээр хаана ч бичээгүй
            учраас нэмэлт тохируулга шаардахгүй. */}
        {subRow}

        {/* DRAWER — `absolute top-full` тул header-ийн доор, хуудсыг ЗӨӨХГҮЙ.
            `px-4` нь капсултай ИЖИЛ 16px гутер — хоёр элемент эгнэнэ. */}
        <div
          className={cn(
            // ⚠️ `transition-[opacity,transform]` БИШ, `translate`. Tailwind v4-т
            // `-translate-y-2` нь `transform` БИШ, ТУСДАА `translate` property-г
            // өөрчилдөг. `transform`-ыг сонсвол дээш гулсах хөдөлгөөн анимацгүй
            // ҮСРЭНЭ (зөвхөн opacity нь зөөлрөнө).
            "absolute inset-x-0 top-full z-50 px-4 pb-4 transition-[opacity,translate] duration-300 ease-out motion-reduce:transition-none",
            open ? "opacity-100" : "pointer-events-none -translate-y-2 opacity-0",
          )}
        >
          {/**
           * `inert` — хаалттай үед панел нь DOM-д ҮЛДДЭГ (хаах анимаци
           * гүйхийн тулд) боловч дотор нь 25+ фокуслаж болох линк бий.
           * `inert`-гүй бол Tab дарахад ХАРАГДАХГҮЙ цэс дундуур фокус
           * "төөрнө". `aria-hidden` нь фокусын дарааллыг АВДАГГҮЙ тул
           * тохирохгүй.
           *
           * `max-h` = 100lvh − 88px (капсулын мөр) − 16px (доод зай)
           * = `calc(100lvh-6.5rem)`. Агуулга үүнээс өндөр бол панел ДОТРОО
           * гүйнэ — дэлгэцээс халихгүй.
           *
           * ⚠️ Гулсалт нь `overflow-hidden`-тэй ДОТООД хайрцагт, гүйлгэлт нь
           * ГАДНАД. Нэг элементэд хоёуланг өгвөл `overflow-x` нь `auto`
           * болж, хоёр давхаргын 200% өргөн нь ХЭВТЭЭ scrollbar гаргана.
           */}
          <div
            ref={panelRef}
            id={DRAWER_ID}
            inert={!open}
            className="bg-card border-border max-h-[calc(100lvh-6.5rem)] overflow-y-auto rounded-[28px] border shadow-2xl"
          >
            <div
              style={{ height: paneH }}
              className="overflow-hidden transition-[height] duration-300 ease-out motion-reduce:transition-none"
            >
              {/* ХОЁР ДАВХАРГЫН ЗАМ — дэд цэс БАРУУНААС зүүн тийш гулсаж
                  орж ирнэ (мөрийг зүүн тийш 50% зөөнө). Буцахад эсрэгээр. */}
              <div
                className={cn(
                  "flex w-[200%] items-start transition-transform duration-300 ease-out motion-reduce:transition-none",
                  subName && "-translate-x-1/2",
                )}
              >
                {/* ─────────── 1-Р ДАВХАРГА ─────────── */}
                <div ref={rootPaneRef} inert={!!subName} className="w-1/2 p-4">
                  {/* ⚠️ `aria-label` нь `<nav>`-д, `<ul>`-д БИШ. Роль-гүй `<ul>`
                      дээрх label-ыг ихэнх screen reader ИЛЭРХИЙЛДЭГГҮЙ. Мөн
                      нэр нь "Үндсэн цэс" БАЙЖ БОЛОХГҮЙ — desktop-ийн
                      `CategoryNav` тэр нэрийг аль хэдийн эзэмшсэн тул хоёр
                      landmark нэг нэртэй болж, цэсний жагсаалт хоёрдоно. */}
                  <nav aria-label="Ангилал">
                    <ul className="flex flex-col">
                      {appleNavCategories.map((cat) => (
                        <li key={cat.name}>
                          {hasSubmenu(cat.name) ? (
                            <button
                              type="button"
                              data-drawer-cat={cat.name}
                              onClick={() => openSub(cat.name)}
                              className={DRAWER_PRIMARY_ROW}
                            >
                              <DrawerCatLabel name={cat.name} active={cat.name === activeName} />
                              {/* Сум = "ДЭД ЦЭС БИЙ" гэсэн цорын ганц тэмдэг.
                                Дэд цэсгүй мөрөнд сум БАЙХГҮЙ тул хэрэглэгч
                                дарахаасаа өмнө "шилжих" ба "задрах"-ыг
                                ялгаж мэднэ. */}
                              <ArrowRight
                                className="ml-auto size-4 shrink-0 opacity-60"
                                aria-hidden="true"
                              />
                              <span className="sr-only">— дэд цэс нээх</span>
                            </button>
                          ) : (
                            <SmartLink
                              href={cat.href}
                              owner={cat.owner}
                              data-drawer-cat={cat.name}
                              onClick={dismiss}
                              className={DRAWER_PRIMARY_ROW}
                            >
                              <DrawerCatLabel name={cat.name} active={cat.name === activeName} />
                            </SmartLink>
                          )}
                        </li>
                      ))}
                    </ul>
                  </nav>

                  {/* ─── ХЭРЭГЛЭГЧИЙН УРСГАЛЫГ ЧИГЛҮҮЛЭХ 2 СОНГОЛТ ───
                      Байгууллага · Unitel Group — эдгээр нь БҮТЭЭГДЭХҮҮНИЙ
                      ангилал БИШ, "та хэн бэ / хаашаа явах вэ" гэсэн ӨӨР
                      ТӨРЛИЙН сонголт. Тиймээс дээрх мега цэснээс ЗАЙГААР
                      тусгаарлав (`mt-4 pt-4`, өмнө нь `mt-2 pt-2` байсан) —
                      зураас нь заагийг, зай нь ЗЭРЭГЛЭЛИЙГ хэлнэ. */}
                  {DIRECT_SEGMENTS.length > 0 && (
                    <div className="border-border mt-4 space-y-0.5 border-t pt-4">
                      {DIRECT_SEGMENTS.map((seg) => (
                        <a
                          key={seg.id}
                          href={seg.href}
                          target={seg.external ? "_blank" : undefined}
                          rel={seg.external ? "noopener noreferrer" : undefined}
                          onClick={dismiss}
                          className={cn(navType.mobileLink, ROW)}
                        >
                          {seg.label}
                          <ArrowUpRight
                            className="ml-auto size-4 shrink-0 opacity-60"
                            aria-hidden="true"
                          />
                        </a>
                      ))}
                    </div>
                  )}

                  {/* ─── ХЭРЭГСЛҮҮД — хэл · профайл · theme ───
                      ⚠️ МӨРӨӨС PILL ЭГНЭЭ БОЛГОВ (2026-09-08, захиалагчийн
                      screenshot). Өмнө нь `ThemeRow`/`LanguageRow`/
                      `AccountRow` гурав нь ангиллуудтай ИЖИЛ өргөнтэй мөр
                      байсан тул навигацийн жагсаалтын үргэлжлэл мэт
                      уншигдаж байв. Одоо гурвуулаа тэнцүү өргөнтэй ТОВЧ —
                      "энэ бол очих газар БИШ, тохиргоо" гэдэг нь хэлбэрээсээ
                      мэдэгдэнэ.

                      ЗУРААСГҮЙ: дэвсгэртэй товч нь жагсаалтаас өөрөө
                      ялгарна, дээр нь зураас нэмбэл давхар зааг болно.

                      ДАРААЛАЛ нь загварынх: хэл → профайл → theme
                      (`HeaderTools`-ийнхоос эсрэг). */}
                  <div className="mt-4 flex items-stretch gap-2">
                    {/* ХЭЛБЭР нь ЭНДЭЭС (`className`) — компонентууд нь
                        `header-shared.tsx`-д, desktop-ийн капсултай
                        ХУВААЛЦАНА. `LanguagePill` нь дарагддаггүй тул
                        `_ACTIVE` (hover) хувилбарыг АВАХГҮЙ. */}
                    <LanguagePill className={cn(DRAWER_TOOL_PILL, navType.mobileLink)} />
                    <ProfilePill className={DRAWER_TOOL_PILL_ACTIVE} onDone={dismiss} />
                    <ThemePill className={DRAWER_TOOL_PILL_ACTIVE} />
                  </div>
                </div>

                {/* ─────────── 2-Р ДАВХАРГА (дэд цэс) ─────────── */}
                <div ref={subPaneRef} inert={!subName} className="w-1/2 p-4">
                  {subMenu && (
                    <DrawerSubmenu
                      menu={subMenu}
                      onBack={() => setSubName(null)}
                      onNavigate={dismiss}
                    />
                  )}
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </>
  );
}

/**
 * 1-р давхаргын ангиллын шошго. ТОДОТГОЛ нь бүх давхаргатай ИЖИЛ хэлбэр:
 * `text-foreground` + доогуур зураас (брэнд ногоон БИШ) — desktop-ийн
 * `CategoryNav`, хувилбар 1-ийн таб, burger-ийн accordion бүгд ижил.
 *
 * Зураас нь ЗӨВХӨН үсгэн доор (мөр бүхэлдээ БИШ) тул мөрийн өндөр хөндөгдөхгүй.
 */
function DrawerCatLabel({ name, active }: { name: string; active: boolean }) {
  return (
    <span className={cn(active && "underline underline-offset-4")}>
      <TabLabel name={name} isDomain={active} />
    </span>
  );
}

/**
 * ДЭД ЦЭС — захиалагчийн 3-р screenshot-ийн бүтэц:
 *   ← Буцах
 *   <Ангиллын нэр>            (чиглүүлэгч гарчиг)
 *   [хурдан үйлдэл]           (байвал)
 *   <Багц>                    (бүлгийн шошго)
 *   Premium · Priority · …
 *   <Бусад үйлчилгээ>
 *   Нэмэлт дата · …
 *   ─────────────
 *   [урамшууллын блокууд]     (дугуй зураг + гарчиг + CTA)
 *
 * ⚠️ ГАРЧИГ НЭМЭГДСЭН (загварт байхгүй). Swisscom-ийн цэсэнд "Back"-ийн дараа
 * шууд жагсаалт орно — тэдний жагсаалт нэг л төрлийн (Mobile-ийн дэд төрлүүд)
 * тул контекст нь өөрөө ойлгомжтой. Бидний жагсаалт нь "Premium · Priority ·
 * Plus" — эдгээр нь ямар брэндийн юу болох нь ЗӨВХӨН гарчгаас мэдэгдэнэ.
 * Мөн `aria-labelledby`-д хэрэгтэй: screen reader хэрэглэгч шинэ давхаргад
 * орохдоо "хаана орсноо" сонсох ёстой.
 */
function DrawerSubmenu({
  menu,
  onBack,
  onNavigate,
}: {
  menu: MegaMenu;
  onBack: () => void;
  onNavigate: () => void;
}) {
  const headingId = `${DRAWER_ID}-${menu.name}-heading`;

  /**
   * ⚠️ ЭХНИЙ САЛАА АНХНААСАА НЭЭЛТТЭЙ (2026-09-10, захиалагч: "Unitel дээр
   * default-р үндсэн багцыг нээлттэй байлгамаар байна").
   *
   * ЯАГААД `menu.name === "Unitel"` ГЭЖ ХАТУУ БИЧЭЭГҮЙ ВЭ: desktop-ийн
   * `BranchedMegaPanel` нь ХОЁУЛАН БРЭНД дээр `menu.sections[0]`-ыг
   * идэвхтэйгээр эхлүүлдэг. Мобайл дээр брэнд шалгавал хоёр давхарга,
   * хоёр брэнд дөрвөн өөр зан гаргана. Тиймээс ижил дүрэм: "эхний
   * ЗАДАРЧ ЧАДАХ салаа нээлттэй".
   *   Unitel    → "Үндсэн багцууд" (захиалагчийн хүссэн зүйл)
   *   Univision → "Интернэтийн шийдэл" (эхний нь `core` бол `groups`-гүй
   *               тул алгасагдана)
   *
   * ⚠️ `defaultValue` (uncontrolled) — `value` БИШ. Хэрэглэгч нээсэн/хаасан
   * бүхнээ өөрөө удирдана; бид зөвхөн ЭХНИЙ төлөвийг өгнө.
   */
  const defaultOpen = menu.sections.find((s) => s.groups?.length)?.id;

  return (
    <section aria-labelledby={headingId}>
      {/* БУЦАХ — панелийн ХАМГИЙН ДЭЭД мөр. Сум нь ЗҮҮН тийш: агуулга
          баруунаас орж ирсэн тул буцах чиглэл нь зүүн — хөдөлгөөн ба
          тэмдэг нь нэг чиглэлийг заана. */}
      <button
        type="button"
        data-drawer-back
        onClick={onBack}
        className={cn(
          navType.mobileLink,
          "hover:bg-muted text-muted-foreground hover:text-foreground mb-1 -ml-2 inline-flex min-h-11 items-center gap-2 rounded-lg px-2 transition-colors",
        )}
      >
        <ArrowLeft className="size-4 shrink-0" aria-hidden="true" />
        Буцах
      </button>

      <h2 id={headingId} className={cn(navType.drawerHeading, "text-foreground mb-1 px-2")}>
        {menu.name}
      </h2>

      {/* ⚠️ ХУРДАН ҮЙЛДЛИЙН МӨР ХАСАГДСАН (2026-09-08, захиалагчийн заавар).
          Гарчгийн доор 2 текст линк байв. Desktop (`BrandMegaPanel`) ба
          хувилбар 1 (`SectionMenu`)-ээс ч ХАМТ хасагдсан — гурав нь нэг
          `quickActions` дата уншдаг байсан. */}

      {/**
       * ⚠️ ХОЁР ПАНЕЛТ ДАТАГ МОБАЙЛД ХЭВТЭЭ БИШ, БОСООГООР (2026-09-08).
       *
       * Desktop-д зүүн 6 мөр тогтмол, баруун панел hover-оор солигддог
       * (`BrandMegaPanel > BranchedMegaPanel`). Мобайлд тэр хоёр панелыг
       * зэрэгцүүлэх өргөн БАЙХГҮЙ, мөн hover ч байхгүй. Тиймээс ижил
       * ДАТАГ нэг гүйдэг жагсаалт болгож дэлгэв: ангиллын мөр, доор нь
       * (байвал) бүлгийн шошго + дэд линкүүд.
       *
       * `groups`-гүй ангилал нь ердөө нэг мөр — desktop дээрх "агуулга
       * тодорхойлогдоогүй" слот мобайлд ХЭРЭГГҮЙ: мөр өөрөө линк тул
       * хоосон панел харуулах шаардлага гарахгүй.
       */}
      {/**
       * ⚠️⚠️ БҮГД ЗАДАРСАН ЖАГСААЛТ → ACCORDION (2026-09-10, захиалагч:
       * "хувилбар 3 дээр дэд category буюу үндсэн багцууд, гэр интернет гэх
       * мэтийг extent хийдэг байдлаар … хэрэглэгч дарах үед нээгддэг").
       *
       * ӨМНӨ НЬ: салаа бүрийн БҮХ бүлэг, БҮХ линк нэг дор задарсан байдаг
       * байв. Unitel-д тэр нь 6 салаа × дэд линкүүд = дэлгэц дүүрэн гүйлт;
       * хэрэглэгч хайж байгаа ангиллаа олохын тулд бүхнийг өнгөрөөх ёстой.
       *
       * ОДОО: `groups`-тай салаа нь ХААЛТТАЙ accordion мөр (chevron-той),
       * дарахад л агуулга нь задарна. `groups`-гүй салаа нь ӨМНӨХ ШИГЭЭ
       * ердөө нэг ЛИНК — дарвал шууд шилжинэ (chevron БАЙХГҮЙ тул хэрэглэгч
       * "задрах" ба "шилжих"-ийг дарахаасаа өмнө ялгана).
       *
       * ⚠️ `type="multiple"` + анхны төлөв ХООСОН: footer-ийн мобайл
       * accordion-тай ЯГ ижил зан (`footer.tsx`). Нэгийг нээхэд нөгөө нь
       * хаагдвал харьцуулж үзэх боломжгүй болно.
       *
       * ⚠️ САЛААНЫ `href` РУУ ОРОХ ЗАМ БАЙХГҮЙ БОЛСОН: өмнө нь салааны мөр
       * өөрөө линк байсан, одоо accordion-ы trigger буюу ТОВЧ.
       *
       * ЭНЭ НЬ ОДООГООР ЮУ Ч АЛДАГДУУЛААГҮЙ: салаануудын `href` нь бүгд
       * `"/#"` — 2026-09-09-ны шийдвэрээр нүүрнээс бусад бүх зам
       * ажиллахгүй болсон (`data/navigation.ts`). Өөрөөр хэлбэл тэр линк
       * хаашаа ч аваачихгүй байсан.
       *
       * ⇒ Жинхэнэ замууд орж ирэх үед ЭНД дахин шийднэ (trigger-ийг линк +
       * тусдаа chevron товч болгох г.м). ӨӨРӨӨСӨӨ "Бүгдийг үзэх" гэх мэт
       * мөр БҮҮ НЭМ — 2026-09-10-нд нэмээд захиалагч хассан.
       */}
      <Accordion type="multiple" defaultValue={defaultOpen ? [defaultOpen] : []} className="mt-3">
        {menu.sections.map((branch) =>
          branch.groups?.length ? (
            <AccordionItem key={branch.id} value={branch.id} className="border-b-0">
              <AccordionTrigger
                className={cn(
                  DRAWER_PRIMARY_ROW,
                  // Chevron нь ӨӨРӨӨ баруун ирмэгт (`ml-auto` анхны утга) —
                  // 1-р давхаргын `ArrowRight`-тай ижил байрлал тул хоёр
                  // давхарга нэг хэлээр ярина.
                  "py-0 hover:no-underline",
                )}
              >
                {branch.title.trim()}
              </AccordionTrigger>

              {/* ⚠️ `[&_a]:no-underline` ЗААВАЛ. `AccordionContent` нь анхны
                  утгаараа `[&_a]:underline`-тай бөгөөд тэр нь DESCENDANT
                  сонгогч тул линк дээрх ӨӨРИЙНХ нь `no-underline`-ыг
                  спецификээрээ ЯЛНА. Тиймээс ЯГ ижил хэлбэрээр дарж,
                  tailwind-merge-ээр солиулна. (2026-09-10-нд accordion болгох
                  үед мартаж, бүх линк доогуур зураастай болсон.)
                  Footer-ийн мобайл accordion ч ЯГ ижил заслыг хэрэглэдэг —
                  `footer.tsx`-ийн `AccordionContent`-ийг үз. */}
              <AccordionContent className="pb-0 [&_a]:no-underline">
                {/* АНГИЛЛЫН СТАТУС — desktop-той НЭГ бичвэр
                    (`BranchStatusNote`). `"link-only"` бол `null` буцаана. */}
                {branch.status && <BranchStatusNote status={branch.status} className="mb-1 pl-5" />}

                {branch.groups.map((group) => (
                  // `pl-3` — дэд бүлэг нь ангиллынхаа ДОР харьяалагдаж
                  // байгааг догол мөрөөр л хэлнэ (зураас, хүрээ нэмэхгүй —
                  // цэс шуугиантай болно).
                  <div key={group.id} className="mt-1 pl-3">
                    {group.title && (
                      <p className={cn(navType.groupLabel, "mb-1 px-2")}>{group.title}</p>
                    )}
                    <ul aria-label={group.title} className="flex flex-col">
                      {group.items.map((item) => (
                        <li key={item.id}>
                          <DrawerLink
                            href={item.href}
                            onNavigate={onNavigate}
                            className={cn(
                              navType.mobileLink,
                              ROW,
                              "text-muted-foreground no-underline",
                            )}
                          >
                            {item.title.trim()}
                          </DrawerLink>
                        </li>
                      ))}
                    </ul>
                  </div>
                ))}
              </AccordionContent>
            </AccordionItem>
          ) : (
            // ДЭД АГУУЛГАГҮЙ САЛАА — accordion БИШ, ердөө линк мөр.
            <div key={branch.id}>
              <DrawerLink href={branch.href} onNavigate={onNavigate} className={DRAWER_LEAF_ROW}>
                {branch.title.trim()}
              </DrawerLink>
              {branch.status && <BranchStatusNote status={branch.status} className="mt-1 pl-5" />}
            </div>
          ),
        )}
      </Accordion>

      {menu.extras && menu.extras.length > 0 && (
        <DrawerGroup label={menu.extrasLabel ?? "Нэмэлт"}>
          {menu.extras.map((s) => (
            <li key={s.id}>
              {/* Туслах жагсаалт — үндсэнээсээ НИМГЭН (14px · 500 · muted).
                  Зэрэглэл нь `SectionRow`-той ижил чиглэлтэй. */}
              <DrawerLink
                href={s.href}
                onNavigate={onNavigate}
                className={cn(navType.mobileLink, ROW, "text-muted-foreground no-underline")}
              >
                {s.title.trim()}
              </DrawerLink>
            </li>
          ))}
        </DrawerGroup>
      )}

      {/* УРАМШУУЛАЛ — навигаци БИШ ӨӨР төрлийн агуулга тул зураасаар заагласан
          (жагсаалтуудын хооронд зураас байхгүй, зөвхөн энд). */}
      <div className="border-border mt-4 border-t px-2 pt-4">
        <h3 className={cn(navType.groupLabel, "mb-3")}>{MENU_PROMOS_HEADING}</h3>
        <div className="flex flex-col gap-4">
          {menuPromos(menu.name).map((promo) => (
            // `key` нь одоо `promo.id` — өмнө нь агуулга placeholder бөгөөд
            // хоёр мөр ЯГ ижил бичвэртэй байсан тул index хэрэглэж байв.
            // ⚠️ `items-center` → `items-start`: тайлбар нэмэгдэхэд мөр нь
            // 3-4 мөрт болох тул дугуйг голлуулбал гарчгаас доогуур унана.
            <div key={promo.id} className="flex items-start gap-3">
              <MobilePromoAvatar image={promo.image} />
              <div className="flex min-w-0 flex-col items-start">
                <p className={cn(navType.secondaryLink, "text-foreground")}>{promo.title}</p>
                {/* НЭГ ӨГҮҮЛБЭР — desktop-той нэг эх сурвалж (`MENU_PROMOS`). */}
                <p className={cn(navType.body, "text-muted-foreground mt-1 line-clamp-3")}>
                  {promo.description}
                </p>
                <Link
                  href={promo.href}
                  onClick={onNavigate}
                  className="text-primary mt-1.5 inline-flex items-center gap-1.5 text-xs font-semibold no-underline"
                >
                  <ArrowRight className="size-3.5 shrink-0" aria-hidden="true" />
                  {promo.ctaLabel}
                </Link>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}

/** Дэд цэсний бүлэг — шошго + жагсаалт. Бүлэг нь `<ul>`-ийн нэрлэгдсэн хүрээ. */
function DrawerGroup({ label, children }: { label: string; children: React.ReactNode }) {
  return (
    <div className="mt-3">
      <p className={cn(navType.groupLabel, "mb-1 px-2")}>{label}</p>
      <ul aria-label={label} className="flex flex-col">
        {children}
      </ul>
    </div>
  );
}

// ─────────────────────────────────────────────────────────────────────
// DRAWER-ИЙН ХЭРЭГСЛИЙН PILL-УУД (хувилбар 3)
// ─────────────────────────────────────────────────────────────────────

/**
 * Гурван pill-ийн НЭГ хэлбэр.
 *
 * `flex-1` + `basis-0` — гурав нь ЯГ тэнцүү өргөнтэй. `flex-1` дээр
 * `min-w-0` шаардлагагүй: агуулга нь хамгийн ихдээ дүрс + "MN" тул 44px-ийн
 * доод хэмжээнээс халихгүй.
 *
 * `min-h-11` (44px) — WCAG 2.5.8. Загварын товч үүнээс арай хавтгай харагдах
 * ч мобайлд хуруунд таарах нь чухал.
 *
 * `rounded-xl` (12px) — капсулын `rounded-full`-аас ЗОРИУД өөр: эдгээр нь
 * navigation БИШ тохиргоо, өөр хэлбэрийн бүлэгт хамаарна.
 */
const DRAWER_TOOL_PILL =
  "bg-muted text-foreground focus-visible:ring-ring inline-flex min-h-11 flex-1 basis-0 items-center justify-center gap-2 rounded-xl transition-colors focus-visible:ring-2 focus-visible:outline-none";

/** Дарагддаг pill — hover нэмнэ. `LanguagePill` нь placeholder тул авахгүй. */
const DRAWER_TOOL_PILL_ACTIVE = cn(DRAWER_TOOL_PILL, "hover:bg-muted/70");

/**
 * ⚠️ `useThemeSwitch` · `LanguagePill` · `ProfilePill` · `ThemePill` ДӨРВҮҮЛЭЭ
 * `header-shared.tsx` РУУ ЗӨӨГДСӨН (2026-09-09). Desktop-ийн капсул ч ЯГ
 * ижил гурван pill хэрэглэдэг болсон тул хоёр давхарга нэг эх сурвалжаас
 * уншина. ХЭЛБЭР нь дуудагчаас (`className`) — эндээс `DRAWER_TOOL_PILL`
 * дамжуулна, desktop нь өөрийн дугуй хэлбэрийг өгнө.
 */

/**
 * Drawer доторх линк — дотоод/гадаадыг ӨӨРӨӨ шийднэ.
 *
 * `SmartLink` БИШ: тэр нь брэндийн `owner`-оор домэйн хооронд шийддэг бөгөөд
 * дэд цэсний элементүүдэд `owner` талбар БАЙХГҮЙ (`MegaMenuSection` нь
 * `{id,title,href}` л). Гадаад эсэхийг href-ээс шалгах нь `SectionRow`
 * (хувилбар 1)-тэй ижил логик.
 */
function DrawerLink({
  href,
  onNavigate,
  className,
  children,
}: {
  href: string;
  onNavigate: () => void;
  className?: string;
  children: React.ReactNode;
}) {
  if (href.startsWith("http")) {
    return (
      <a
        href={href}
        target="_blank"
        rel="noopener noreferrer"
        onClick={onNavigate}
        className={className}
      >
        {children}
        <ArrowUpRight className="ml-auto size-3.5 shrink-0 opacity-60" aria-hidden="true" />
      </a>
    );
  }
  return (
    <Link href={href} onClick={onNavigate} className={className}>
      {children}
    </Link>
  );
}

/**
 * Цэсний ангиллын шошго — LookTV нь ОНЦГОЙ (`SparklesText`).
 *
 * ⚠️ `morph` PROP ХАСАГДСАН (2026-09-14). Түр зуур dock-ийн LookTV-г
 * `MorphingText` ("LookTV" ⇄ "Илүүг Үз") болгосныг захиалагч буцаав —
 * "sparkling-аар үлдээе". ⇒ БҮХ давхаргад (dock ба drawer) НЭГ хэлбэр:
 * очнууд бүжинэ, бичвэр нь ТОГТМОЛ.
 */
function TabLabel({ name, isDomain }: { name: string; isDomain: boolean }) {
  return (
    <>
      {/* ⚠️ LOOKTV — `SparklesText`-ийн тохиргоо нь desktop-ийн `CategoryNav`
          -тай ЯГ ижил (8 оч, брэндийн ногоон) — хоёр давхарга өөр харагдвал
          нэг хуудсан дээр хоёр өөр зан гарна. Шалтгаан ба түүхийг
          `header.tsx`-ийн `label`-ийн тайлбараас үз. */}
      {name === "LookTV" ? (
        <SparklesText
          sparklesCount={8}
          colors={{
            first: "var(--primary)",
            second: "color-mix(in oklab, var(--primary) 45%, white)",
          }}
        >
          {LOOKTV_MORPH_TEXTS[0]}
        </SparklesText>
      ) : (
        name
      )}
      {isDomain && <span className="sr-only"> (одоо байгаа домэйн)</span>}
    </>
  );
}

/**
 * Mobile-ын урамшууллын дугуй — desktop-ийн `PromoAvatar`-тай ижил үүрэг,
 * гэхдээ `size-12` (48px). Desktop нь 56px: 375px дэлгэцэнд 56px дугуй нь
 * текстэд 280px-аас бага зай үлдээж гарчгийг гурван мөр болгодог.
 */
function MobilePromoAvatar({ image }: { image?: string }) {
  // ⚠️ Зураг БАЙВАЛ бодит кампанийн зураг (2026-09-08 — `MENU_PROMOS` нь
  // одоо `promotionCards`-аас бодит дата уншдаг). Univision-ы кампанийн дата
  // одоогоор placeholder бөгөөд зурaггүй тул тэр build дээр `Gift` хэвээр.
  // `sizes="48px"` — кампанийн зураг 1000px+ өргөнтэй тул үүнгүйгээр Next
  // 100vw гэж үзэж дэмий том файл татна.
  if (image) {
    return (
      <span
        aria-hidden="true"
        className="bg-muted relative size-12 shrink-0 overflow-hidden rounded-full"
      >
        <Image src={image} alt="" fill sizes="48px" className="object-cover" />
      </span>
    );
  }

  return (
    <span
      aria-hidden="true"
      className="bg-muted text-muted-foreground flex size-12 shrink-0 items-center justify-center rounded-full"
    >
      <Gift className="size-5" />
    </span>
  );
}

// =====================================================================
// ХУВААЛЦАХ ХЭЛБЭР
// =====================================================================

/**
 * Цэсний бүх мөрийн НЭГ хэлбэр — ингэснээр хоорондын зай жигд болно (40px).
 *
 * ⚠️ Энэ тогтмолын хамт байсан `MenuSheet` (баруунаас гарах `Sheet` цэс) ба
 * түүний `SubItem` · `ThemeRow` · `LanguageRow` · `AccountRow` дөрөв нь
 * 2026-09-14-нд БҮРМӨСӨН УСТСАН: хоёулаа хувилбар одоо `BurgerDrawerHeader`
 * -ийн хоёр давхаргат drawer-ыг хэрэглэнэ (захиалагч: "header болгон дээрх
 * burger menu-г … хувилбар 1 дээр шингээгээд"). Одоо `ROW`-г `DrawerSubmenu`
 * л хэрэглэнэ. Sheet хувилбарыг сэргээх бол git түүхээс.
 */
const ROW =
  "hover:bg-muted flex w-full items-center gap-2 rounded-md px-2 py-2.5 text-left transition-colors";
