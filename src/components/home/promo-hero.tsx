import { PromoBanner } from "@/components/sections/promo-banner";

/**
 * НҮҮРНИЙ PROMO HERO — ЗӨВХӨН promo carousel. ХОЁР БРЭНД ХУВААЛЦАНА.
 *
 * ⚠️ Өмнө нь `UnitelHero` нэртэй, ЗӨВХӨН Unitel-д хэрэглэгддэг байв.
 * Univision нь `<PromoBanner />`-ыг ШУУД дуудаж, banner нь өөрийн
 * `min-h-[38svh]` (≈296px) өндрөө хэрэглэдэг байсан — Unitel-ийн 60%
 * (≈500px)-аас хамаагүй намхан. Хоёр нүүрний эхний дэлгэц зөрж байсан
 * тул hero-г брэнд-нейтрал болгож НЭГ формул болгосон.
 *
 *   ┌─────────────────────────────┐
 *   │  header (хөндөөгүй)         │  ← `relative`, sticky БИШ
 *   ├─────────────────────────────┤
 *   │  ┌───────────────────────┐  │
 *   │  │  PROMO CAROUSEL       │  │  60% × (100svh − header)
 *   │  │              ←  2/3 → │  │
 *   │  └───────────────────────┘  │
 *   ├─────────────────────────────┤
 *   │  AI assistant (`ChatHero`)  │  ← үлдсэн зайд эхэлж, доош үргэлжилнэ
 *   └─────────────────────────────┘
 *
 * ⚠️ "САНАЛ БОЛГОХ БАГЦ" ЭНДЭЭС ГАРСАН. Өмнө нь энэ hero нь promo 60% +
 * багц 40% гэж НЭГ дэлгэцийг хуваадаг байв. Одоо багцын хэсэг нь ТУСДАА,
 * бүрэн хэмжээний section болж AI туслахын ДООШ шилжсэн — тэгснээр карт нь
 * дэлгэцийн 40%-д шахагдахаа болив.
 *
 * ӨНДӨР
 *   мобайл — `56svh`: promo нь эхний дэлгэцийг эзэлж, доорх AI туслах
 *            хэсэгчлэн харагдана (доош гүйлгэхийг урина).
 *   md+    — `(100svh − header) × 0.6`: өмнөх 60%-тай ЯГ ижил өндөр, тул
 *            баталгаажсан харагдац хэвээр.
 *
 * ⚠️ Дээд зай (`pt-`) нь ӨНДРИЙН ДОТОР — section-ий нийт өндөр
 * өөрчлөгдөхгүй, зөвхөн banner нь тэр хэмжээгээр намхан болно. Тиймээс
 * доорх AI туслахын 40%-д НӨЛӨӨЛӨХГҮЙ, эхний дэлгэц хэвээр багтана.
 *
 * `100svh` (`vh` БИШ) — мобайл хөтчийн URL bar задарсан үеийн ЖИЖИГ viewport.
 * `--header-h`-г `HeaderHeightVar` бичнэ; globals.css-д SSR-ийн fallback байна.
 *
 * ⚠️ `<section>` байх ЁСТОЙ: `SectionSnapScroller` нь `#main-content > section`
 * гэж шууд хүүхдийг хайдаг — `<div>` болговол snap ажиллахгүй.
 */
export function PromoHero() {
  return (
    <section
      aria-label="Онцлох санал"
      // ⚠️ МОБАЙЛД ӨНДӨР ЗААГААГҮЙ (өмнө нь `h-[56svh]`). Загварын карт нь
      // 342×456 = ЯГ 3:4 харьцаатай тул өндрийг нь өргөнөөс гаргах нь зөв:
      // 390px дэлгэцэнд 456px гарна, бусад өргөнд харьцаагаа хадгална.
      // svh-д тулгуурлавал өргөн дэлгэцэнд карт нь загвараас ТОВГОР болно.
      className="w-full pt-[clamp(1rem,3svh,2rem)] md:h-[calc((100svh-var(--header-h))*0.6)]"
    >
      <PromoBanner fill />
    </section>
  );
}
